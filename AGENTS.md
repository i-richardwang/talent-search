# talent-search 开发约定

运行方式、模块职责与数据来源见 [README.md](README.md)；产品定位与范围见 `docs/PROJECT.md`。

**这份文件只放代码里放不下的约定。** 三类，没有第四类：

1. **禁令**——它管的代码不存在，所以没有任何地方可以注释它；
2. **跨文件的不变量**——没有任何一个文件是它的主人，写在谁那里都只说了一半；
3. **全绿的陷阱**——违反之后 `tsc` / `biome` / `build` 全过，只有人眼或用户能发现。

「某个文件为什么这么写」一律写在那个文件里，这里不复述。复述出来的那一份不受类型检查、
不受测试保护，改代码的人不会顺手改它，于是它只会烂——而写在文件里的同一批论证会跟着
代码一起改。界面这一章尤其危险：换一次组件库就能让几百行描述性的条款集体失效，
而它们失效的时候没有任何东西会红。**看到一条规则能被移进某个文件的注释里，
就移过去，然后把这里删掉。**

## 数据

- **真实员工数据不进版本库。** 同步只从配置好的数据源读、只往 Postgres 写。不要把
  parquet、导出的 CSV、查询结果或含真实姓名工号的样例写进仓库或文档。仓库里能出现的
  只有合成数据（`src/corpus/sources/sample/`、`tests/fixture.ts`）。
  - **测试也不许碰真数据源。** `bun run test` 带着 `.env.local` 跑，而那里面指的是真的
    人事数据；夹具因此把 `TALENT_SOURCE` 钉死在合成样例上。不钉的话，一次跑测试就是
    把真语料读一遍、嵌一遍，而它全绿。
- **数据源与管线之间只有一份契约**（`src/corpus/contract.ts`）。分界线是一句话：**换一套数据
  就得重写的，归适配器；换一套数据也必须成立的，归管线。** 管线里出现任何一个具体源的
  字段名，下一个接数据的人就会照着再写一套「什么算一段经历」。
  - **适配器默认是私有的。** `.gitignore` 用白名单放行 `csv-dir` 与样例，其余一律不进
    仓库——表名和字典码本身就是一家组织的内部信息。规则的方向很重要：新写一个适配器
    **天然**被忽略，「忘了加 ignore」这件事不可能发生。
  - **按名字取适配器只能用带变量的动态 import**（`src/corpus/sources.ts`），写法要留在
    Vite 认得的那一种：`./` 开头、只差一级文件名、带扩展名。换成一张手写的注册表，
    那张表就得列出私有适配器，而它在版本库里——干净克隆当场构建失败。
  - **管线不许为某一个源开分支。** 源之间的差异用契约的取值表达（比如 `segment_key`），
    不用 `if source == ...`。
- **公开表结构的唯一事实源是 `src/db/schema.ts`**，改完跑 `bun run db:push`；**不生成
  迁移文件**——两套改表机制并存就是同一件事两个入口。同步不改结构，只从公开表 `LIKE`
  出事务私有的暂存表做差。pg-boss 的 `pgboss` schema 不归它管，首次启动自建。
  - **改 check 的表达式必须同时改约束名。** `db:push` 不比较 check 的表达式，同名改表达式
    它报「已应用」而库里还是旧的；`tsc`、测试（临时 schema 是新建的）全绿，只有发布时被
    旧约束报错才发现。`experience_phrase_route_valid_6` 的后缀就是为此。
- **语料是增量长的，三种任务写，一次只有一个写者。** 同步（`src/corpus/sync.ts`）一笔
  事务增删人和段，段按内容键认（`experience.key`，库算）；派生（`derive.ts`）几百段一批、
  每批一笔短事务写说法、边和段上的版本；整理（`vocabulary.ts`）改词表并把边改指标准词。
  三者由会话级 advisory lock 串行化（`src/corpus/session.ts`）：后台任务拿不到就下一轮再来，
  命令行的同步排队等。不另写第四种写者，也不让谁绕开这把锁。
  - **段的身份是内容，派生的版本是依赖的摘要。** 同步不算摘要，把行灌进 `like ... including
    generated` 的暂存表让库算，两张表按键做差——应用侧再算一遍键，两处漂开就是每次同步整库
    重插而没有报错。`derived_identity` 是抽取与对齐的提示词、模型和嵌入空间的摘要，改任何
    一样等于所有段待派生，不要另加一个「要不要重跑」的开关。
  - **跨语句读取只走 `withCorpusSnapshot`**，它跑在 repeatable read 里：一次检索看到的
    是某一笔提交之后的整体，不会一半新边一半旧边。`embedding_space` 那一行只在换嵌入空间
    时重写，行版本变了就是说法整张换过了，准入要重算。
  - **模型调用（嵌入、重排、查询理解、抽取）不许发生在任何事务里**——占着连接、占着锁。
    派生在事务外算完一批再开事务落库；准入在快照外算完再进快照取数。
    论证写在 `src/db/index.ts` 与 `src/search/phrases.ts` 的 `withAdmission` 里，这里不复述。
  - **说法表只增不改。** 段没了边级联没了，说法留着；id 稳定，重排分数的缓存跟着稳定。
    唯一清它的时刻是换嵌入空间（`derive.ts` 的 `ensureSpace`）。
  - **写者独占一条连接，用完必须收尾。** 锁是会话级的，因为派生要跨过几百笔事务。收尾写在
    `src/corpus/session.ts` 的 `release` 里，收不了尾的连接直接销毁、不还回池子。
- **drizzle 是 schema 与迁移工具，不是查询层。** 检索的 SQL 一律手写，集中在 `phrases.ts`
  （召回要 `lateral` 取每个词的前 N 个说法）和 `search.ts`（同段去重要 `distinct on`）：
  查询构建器两样都做不好。查询构建器只用在
  **按主键取实体**那一类（`functions.ts` 取单人时间线）。这条不写下来，下一个人顺手用它
  写一条检索，两种风格就并存了，而没有任何关卡会拦。
- **拒绝必须报告。** 管线遇到日期倒置、缺开始日、生效日在未来的源记录只报告并拒绝，
  不猜日期，也不靠 `Math.max(1, ...)` 把错误区间伪装成一月经历。静默丢数据的同步没人能
  验收——而「验收」是有具体去处的：任务说过的每一行都进 `task_run.log`，命令行和
  任务台 `/tasks` 读的是同一份。
- **不落没有读者的列。** 库里每一列都要能指出谁在读它（检索谓词或界面上的某处）。切段时的
  工作值（去重键、回填入口、匹配别名）算完即弃，不进库：存下来之后，下一个人无从判断它是
  废弃的还是漏用的。这条同样适用于 `org_meta` 里的 key。
- **不为「将来可能要」预留任何东西**：不留没人设置的类型字段、没人调用的导出、没有第二个
  使用者的 `export type`、没跑过的脚本。新能力在需求和读者真实出现时再加。

## 检索

- **限制只写在收窄的地方，不写在准入的地方。** 同一件事不许有两份契约：能收窄的一律收窄，
  不要把同一个上限再写成 schema 约束——那样模型多给一个长词就是整条响应作废、整句理解
  失败，而收窄本来只会丢掉那一个词（词长这一例的论证在 `term.ts` 的 `MAX_TERM_LEN`）。
- **值域不许写成针对性枚举。** 筛选栏里那几个档是**分面的桶**，是展示，不是数据层的值域
  （`MIN_MONTHS_BUCKETS` 旁边写着为什么）。凡是「界面上只有这几个选项」就去锁死数据层
  值域的，都是把展示当成了契约。
- **打分不许写进 SQL。** `search.ts` 只产出事实，打分、AND 判定、排序、分面全在 `rank.ts` 里
  对内存中的事实求值。唯一的 SQL 内强度比较是**同一要求、同一经历段的去重**，它不产生人员
  分数、不做准入或排序。换检索手段时，人员排名与分面仍只改 `rank.ts`。
- **匹配系数只许住在 `src/search/weights.ts`**，不要散进别处。各个常量为什么是那个值、
  两个下限的乘积为什么必须大于 0.5、`RELEVANCE_MIN` 的数从哪来，都写在那个文件里；
  命中分两步、两个模型各管一头也写在那里。
- **成员资格不为任何一个词开特例。** 某个词搜错了（「产品经理」匹配到「销售经理」），先问它
  暴露的是哪条机制的问题；机制修不了的残余错误由证据行暴露给用户改词——**产品代码里
  不许出现某个具体的词**，也不许逐查询、逐路、逐筛选去动那条全站阈值。
- **正向要求可以模糊，排除词必须准。** 正向要求按 `RELEVANCE_MIN` 判定通过；排除词用更高的
  `RELEVANCE_MIN_EXCLUDE`，而且不带筛选。理由是「错了能不能被看见」：召回错了，人在名单上，
  看一眼就能纠正；否决错了，证据无声消失，永远没人知道。
- **语料侧和查询侧必须是同一个嵌入空间。** 它们调的是同一个模块、读的是同一份配置
  （`src/server/embed.ts`），所以这条不是一句要记住的约定；**跨时间**那一半才需要看住：
  `EMBED_SPACE_ID` 标识模型、权重、归一化与端点实现的组合，任一项变化都换 id，派生会重算。
  派生把 id、模型、维数和 fresh canary 写进 `embedding_space`，查询进程首次嵌入前逐项核对
  并重新嵌 canary；不一致直接拒绝检索，不允许用空名单冒充答案。维数只有
  `src/db/schema.ts` 的 `EMBED_DIM` 一处，没有对应的环境变量——不给它第二个出处。
  重排缓存按 `RERANK_SPACE_ID` 键入；重排行为变化就换 id，换嵌入空间时随 phrase 级联清空旧分。
- **原文四路说法的拼法只有一份实现：`src/corpus/route-texts.ts`。** 序列三级用「 · 」连、
  部门在公司内用 `org_path`、空文本不存向量——这些决定了库里的向量是从什么字符串来的。
  派生（`derive.ts`）和造语料的测试夹具（`tests/fixture.ts`）**都 import 它**，谁也不许
  自己再拼一遍：拼两遍的那一刻起，夹具嵌的和线上嵌的就可能不是一种字符串，而测试全绿。
- **专有名词永远不进向量。** 公司名、学校名在向量空间里和同类名字是邻居，语义匹配会把竞品
  一起匹配进来；它们走 `org` / `school` 两个精确条件（`ILIKE`，输入必须过 `escapeLike`）。
  这条的执行点在查询的形状里（`Term.field` 把 org / school 和 experience 列成互斥的维度），
  不在检索代码里——检索代码拿到什么词就嵌什么词。
- **查询的唯一表示是 `Term[]`（`term.ts`），模型嘴里、库里、RPC 上、屏幕上都是它。**
  一条 Term 是「在哪一维找什么」：`field`、`mode`、`values`，取值之间 OR、条件之间 AND；
  经历词和范围维度是同一种条件、同一枚 chip、同一个菜单。模型的任务是**写这条搜索**，
  不是理解句子——它交出来的对象不经翻译就是记录。模型输出、RPC 入参走同一个收窄口
  `termsOf`；改一条就是对数组做一次 map 或 filter。`大模型/推荐系统,+org:字节` 这样的
  一行字只是命令行、验收用例和测试夹具的输入便利（`query-syntax.ts`），产品里没有一处读写它。
- **搜索词是模型对「要找什么人」的表达，不是用户的原话。** 用户说「搞推荐的」，模型写
  「推荐算法」「推荐系统」；一条条件里的几个取值同权，靠哪个命中只进证据行（「≈ 那个词」），
  不进分。不给取值标来源，不让模型把原话交回来核对——为什么，见 `term.ts` 头注释。
- **库里表达不了的条件不解释，直接不写。** 「北京的」没有对应的维度，「资深」对不上职级词表，
  搜索产品对说不清的条件只有两种处理：软化成排序信号（「资深」是职级上的 boost），或者不管。

## 大模型

- **判断进提示词，阈值与权重进代码，事实问语料。** 一句话里哪些是条件、落在哪一维，哪种
  语气是哪档强度，是逐查询的判断，归提示词管、可以大改（记录不可变，改坏了不污染旧查询）；
  相似多少算命中、各路值多少分、结构长什么样，是全站的阈值与权重，只住在代码里（跨查询可比、
  有调参把手、用户改得动）；一个词在库里对应多少人，问语料（`probeWide` 量宽，落库前）。
- **模型适配层只有四个文件，都薄到没有逻辑**：`src/server/llm.ts`（查询理解）、
  `src/server/embed.ts`（嵌入）、`src/server/rerank.ts`（重排）、`src/server/chat.ts`（聊天）。
  形状与收窄在 `src/search/intent.ts`（纯函数），召回与判定的编排在 `src/search/phrases.ts`。
  语料侧的四处调用都从这两个适配层出去：`corpus/embed.ts`（嵌入，加库里的缓存）、
  `corpus/extract.ts`（简历描述 → 能力词与做过的事）、`corpus/align.ts`（入职前岗位对到
  公司序列）与 `corpus/vocabulary.ts`（能力词词表），后三处共用 `chat.ts`，收窄各在
  自己的 `conform`。**端点只有适配层碰**：多一个地方 new 一个 provider，就多一份会走样的
  超时、重试与鉴权。想再加一处模型调用之前先问：这件事能不能做成「模型给建议、纯函数
  收窄、结果落在用户改得动的表示上」——不能就别加。
- **抽取出来的东西只是说法，不是第二套语料。** 能力词与做过的事进 `phrase` /
  `experience_phrase`，走和原文四路一模一样的召回、判定与打分；强度只看字段来源，所以它们
  和 `description` 同档（论证在 `weights.ts`）。不给它们另建表、另写一条检索、另设一档权重；
  模型的回答按模型名和提示词键入存在 `completion_cache`，只有派生和整理读它——查询侧没有任何
  东西碰这张表。
  做过的事的说法只是领域，参与方式存在边上（`experience_phrase.involvement`），只在证据行上
  拼成「从零搭建 · 推荐系统」（`evidence.tsx` 的 `phraseLabel`）：枚举不进向量，论证在那一列的注释。
- **能力词存最细的那一档，宽的在查的时候聚出来。** 招聘的人要的粗细是当场定的，语料预知
  不了；细的能聚成粗的，粗的还原不成细的。抽取和整理的提示词都先讲这个用途（谁在筛选栏里点
  这些词、宽细怎么聚、细的为什么不能丢），再给规则：实测讲了用途，「跨部门协作」「发布会物料
  准备」这类没人会点的词就不写了，规则一条条禁禁不全。规则按「模型会照做」来写，只有一版：
  4B 一级的模型不照做规则、只抄例子，写了规则反而把项目名一起留下，但迁就它就得写一条让大模型
  丢掉限定语的错话。词表 `skill_term` 分两层（`src/corpus/vocabulary.ts`）：**同一件事的不同
  写法**归成一个标准词，派生写边时按它换词、整理改了决定就把边改指标准词，这是无损的；**一个词
  属于哪个更宽的词**只记一条 `parent`，细的词原样留在人身上。筛选栏「入职前技能」沿归属往上聚
  （`search.ts` 的 `FACT_COLUMNS.skills`），检索的向量路不读词表——向量早把不同写法放成邻居了。
  词表记的是关于词的决定，不是语料：换数据源、换嵌入空间都不清它，只有整理写它；应用侧只有
  管理页 `/skills` 读它，只读——改的入口下一轮整理就会被机器盖回去。标准写法是同一件事那一片里
  人最多的词，不由裁判选；更宽的词由裁判起名，可以是语料里没人写过的，它自己也落成一行（归属
  指回本表）。**词表里只有一种词**：人写的和裁判起的都是标准词，人数都按它下面的人算（筛选栏
  同一口径），都一样到期出题——出题的词表是表里的标准词加语料里还没进表的词，只从语料取词的话
  起的名字永远不会被再问，挂不到更宽的词下、两组各起的同一个词也并不到一起。去掉限定语后一样的
  两个词是兄弟，各自保留、共一个更宽的词；只是其中一个环节的不属于它。这条线用 `REVIEW_MODEL`
  判，抽取仍用小模型；写坏的表（别名的别名、归属于别名、归属成环）出声拒绝。
  - **判卷可以外包，词表不外包。** 整理一轮是作废、结算、出题、判卷四步，只有判卷这一步
    切得出去（`REVIEW_JUDGE`）：题落在 `skill_review` 上，外部 agent 走 `/api/review` 拉题交卷
    （`src/server/review.ts`），**只写题那一行**。词表和边只在结算时改、在写者锁里、同一笔
    事务——给外部一条改词表的路，就等于给了语料第四个写者，而它绕开了锁。
  - **两种裁判的答卷走同一处收窄（`conform`）。** 外部交上来的比模型交上来的更不可信，
    不给它第二个入口；接口那一侧只把原话原样存进题里，收窄留到结算，改收窄规则不作废
    已经交上来的答卷。判卷标准（`GUIDE`）也只有一份：发给自带模型的和给外部读的是同一段字。
  - **一个词同时只在一道题里，一道题只有第一份答卷算数。** 前者靠出题时把队列里挂着的词
    整个排除在圈组之外，后者靠 `judge is null` 那一句——两份答卷不一致时挑哪一份都得有个
    说法，而「先到的算」是唯一不需要说法的那个。不投票、不仲裁、不给题发租约。
- **对齐的序列只进筛选，不做证据；登记值和推断值不同列。** 入职前的段由模型对到公司序列树，
  写在 `seq_inferred_l1 / l2`，`seq_l1..3` 永远只有 HR 登记值。序列筛选读「登记的，没有才读
  对齐的」（`search.ts` 的 `FACT_COLUMNS`），`seq` 那一路只嵌登记值——推断进了打分就和受控
  字段的可信度分不开。对齐是封闭集合：树上存在的一对，或者两级都空；不允许只对一级，
  序列筛选只认成对的值，只有一级的推断没有读者。
- **四个端点的数据边界不一样，所以部署要求不一样。** 查询理解发出去的只有用户敲的那句话和
  语料里几维筛选的取值（公司档、职级、招聘渠道、学历），走公网 provider 没有顾虑；嵌入端点和
  抽取端点从派生那一侧收到的是**每个人的经历原文**，重排端点在查询时收到召回的岗位名、
  部门路径、简历描述与抽出来的说法，
  放公网等于把经历交给第三方——这是部署方按数据政策做的决定，代码不替它做，但文档必须把
  这个差别说清楚。姓名、工号永远不出这台机器；
  这也是「查询理解做、结果重排不做」的分界线——重排必须把候选人的经历发出去。
- **查询侧的端点没配就抛、失败就抛，不降级。** 一句话只有模型能读成条件，没有第二种读法
  能给出同一份结果——按句式切词读不出语气，「不要实习」会变成必须词，结果是错的而 chips
  看起来完全正常。所以 `understand()` 失败就抛，记录停在待理解，界面画出错误与重试
  （`routes/s/$turnId/-lib/interpret.ts`）。嵌入或重排没了检索什么都做不了，`embed()` /
  `rerank()` 同样没配就抛——装作能用只会返回一份空名单或一份反义名单，而空名单在这个界面里
  的意思是「没有这样的人」。抽取端点没了原文四路照样成立，所以派生**说明后跳过**
  （`derive.ts` 报出两路为空、序列不对齐），不抛也不静默；单段响应异常时丢那一段、不进缓存，
  不让一次抖动回滚整批。
- **模型输出是不可信输入**，必须过 `toSpec`，和 URL 过 `validateView`、服务端函数入参过
  `sanitizeFilters` 是同一档事。上游声明过 schema 不能省掉这一道。
- **模型给的词要过一遍 `termOf`**，和命令行敲的词走同一条边界：限长度，不改写字面。
  多了任何改写，屏幕上写的和拿去比相似度的就不是同一串字。
- **模型挑筛选值只能从语料词表里挑**（`vocabulary()`）。职级尤其如此：P5 到 P9 是这家组织
  的阶梯，另一家可能是 M1 到 M5，「总监」对应哪一档只有词表能回答。词表只在提示词里列
  一遍、不进 schema：在不在词表里由 `toSpec` 查，查不过的取值丢掉、一个不剩的条件整条消失
  ——而不是塞进经历词去和岗位名比相似度，也不是整句作废。词表取值用的是维度自己那条列
  表达式（`FACT_COLUMNS`），不另写一份。
- **提示词的读者是模型，论证写在注释里。** 提示词只说每一维是什么、拿不准时怎么办，
  外加几个完整例子；「为什么公司名不进向量」「为什么不放更宽的词」这类论证写在
  `llm.ts` 的注释和 `term.ts`、`intent.ts` 里。写进提示词的论证没有读者，只会把一份说明变成
  几十行没有例子的劝说；数字（几条、几个字）同样不进提示词，上限住在收窄那一处。
- **提示词是手写的，不从维度表生成。** 加一维时模型不会自动认识它——这是有意的：让模型
  开始产出一个新维度会改变模型行为，得单独验，不能顺手搭在一次重构里。它的失败也看得见
  （模型少认一维），不是静默算错。模型一维只给一个值，落到记录上是这一维的一项。
- 密钥走 `.env.local`，和 `DATABASE_URL` 同一个待遇。查询理解端点只用 POST，输入与模型输出
  都要有硬上限；公开部署时限流必须做在网关层，进程内计数器盖不住多实例。

## 服务端边界

- **服务端模块自己声明身份**：`#/db`、`#/search/search`、`#/search/phrases`、`#/server/llm`、
  `#/server/embed`、`#/server/rerank`、`#/server/chat`、`#/server/turn`、`#/server/tasks`、
  `#/server/jobs`、`#/server/data`、`#/server/skills`、`#/server/review`、`#/server/endpoint`，以及 `#/corpus/` 下碰连接或端点的那几个（`embed`、
  `vocabulary`、`sync`、`derive`、`session`），顶上都有一行 `import "@tanstack/react-start/server-only"`。页面从它们取**值**
  会让构建失败，并打印完整 import 链。新写一个碰数据库连接或密钥的模块，就给它加上那一行。
  `#/db/schema` 不标（`drizzle-kit` 以 CJS 加载它，标了 `db:push` 起不来）；`#/corpus` 里的
  纯函数（`contract`、`pipeline`、`route-texts`、`extract`、`align`、`report`）也不标——
  夹具和页面用得上它们的类型与拼法。
- **判据住在答得出它的那一侧。** `/tasks` 上「这一行是跑着、中断了、失败了还是跑完了」
  写在 `#/server/tasks`：判「正在跑」要问那把咨询锁此刻在谁手上，页面问不到；页面只把
  结论译成字（`routes/tasks.tsx` 的 `SAID`）。页面自己答得出的判据仍然留在页面——从服务端
  模块取一个**值**会把整条 import 链拖进客户端包，构建当场红（排班的 cron 在 `jobs.ts`，
  页面上说给人听的那份另写在 `CADENCE`，就是这个原因）。
- **服务端函数是可以被直接调用的端点。** 页面那侧的 `validateView` 管 URL，`search/params.ts` 的
  `sanitizeFilters` 管进程边界，两道都要有：少了后者，`minMonths: "abc"` 会进入数值比较并
  安静地筛掉所有人。

## 分层

`-` 前缀被路由生成器忽略，是 TanStack Router 的官方约定，它的语义是**「这个路由私有」**。
**按「谁在用」分，不按「像什么」分**，而且按今天的使用者算，不按将来可能：

| 目录 | 装什么 | 判据 |
| --- | --- | --- |
| `src/components/ui/` | 抄来的通用件（按钮、卡片、弹层、`Empty`…），见那里的 `NOTICE.md` | 换个产品照样能用 |
| `src/components/` | 这个产品自己的展示件与视觉词汇（`query-bar` / `evidence` / `timeline` / `career-bar`） | 跨屏共用，但离开人才搜索就没有意义 |
| `src/routes/-components/` | 各屏都要的外壳件（`app-header` / `recent-popover` / `page-frame` / `dead-end` / `zero-state` / `admin-page` / `status-badge`） | 只有路由在用，而且不止一条路由 |
| `src/routes/-lib/` | 各屏都要的非组件模块（`commit` 提交查询、`term-label` 条件→标签） | 同上 |
| `src/routes/s/$turnId/-components/` | 工作台私有的组件（`query-deck` / `query-chips` / `filter-rail` / `result-list`） | 只有这一条路由在用 |
| `src/routes/s/$turnId/-lib/` | 工作台私有的非组件模块（`interpret` / `keyboard-flow` / `media` / `nav-phase` / `view-params` / `filters` / `empty-state`） | 同上 |

**一个东西住哪儿，由今天有几条路由 import 它决定。** 多一条路由要它，就往上挪到共同祖先的
`-components/` 或 `-lib/`；只剩一条路由要它，就往下挪进那条路由自己的目录。让 route-private
之间互相 import（`../../-components/...` 这种爬法）等于目录名在撒谎，下一个人据此判断
「改它会影响谁」就会判错，而这件事没有任何检查会报。

其余几条同性质的分界：

- `src/corpus/` 是**怎么把数据变成语料**，`src/search/` 是**怎么在语料里找人**，两者平行、
  互不 import；它们之间只有 `src/db/schema.ts` 那一份表结构。`src/server/` 在两者之上：
  服务端函数、查询记录、任务的记录与排班和四个模型适配层。判据是**谁读它**——`route-texts.ts`
  在语料侧是因为它决定了库里的向量从什么字符串来，而查询侧只是嵌用户敲的那几个字；
- `src/search/` 内部：`result.ts` 是结果形状（页面可以取值），`search.ts` 带 `db`
  （页面只能取类型）。加字段时先问它属于哪一侧；
- `src/db/index.ts` 只出连接，表定义一律从 `#/db/schema` 取；与界面无关的纯函数进 `src/lib/`。

## 界面

组件库是 coss ui，源码进仓库（`src/components/ui/`）。**升级或补组件之前先读
`src/components/ui/NOTICE.md`**——从哪个目录抄、为什么不能抄错目录，写在那里。

- **凡是「一块面」或「一个控件」，都由 `src/components/ui/` 给。** 卡片、提示条、分隔线、
  按钮、开关、可点的标签——路由组件只挑组件、排布局、喂数据，不自己拼
  `rounded-* + border + shadow-*` 去画一块面。（吸顶的头给自己补一层 `bg-card` 挡住穿过去
  的内容不算画面：那是同一块面的延续，不是新的一块。）
  理由不是洁癖：coss 的身份不在颜色令牌里，在每块面上那层 `before:shadow` 顶光边、带色相的
  `shadow-xs/5`、`pointer-coarse` 触控目标和 `data-pressed` 按压态上。手写的面把颜色抄对了
  也一样都不会有，于是屏幕读起来是通用 Tailwind 而不是 coss——**换组件库最容易漏掉的
  就是这一层**，因为它不在令牌里，只在组件源码里。
- **成块的场景先找它有没有现成的组合件，目录里没有就从注册表补抄一个进来。** 零态、空结果、
  404 都用 `Empty`（媒介 / 标题 / 内容三段，媒介可以不要——那枚带叠卡的图标说的是「这里
  本该有东西」，只属于缺席的那两屏）；一个框和它的提交按钮是同一块面，用 `InputGroup` 加
  `InputGroupAddon`，不是并排的两块；提示条 `Alert`、卡片 `Card`、开关 `Toggle`。自己用 flex
  摆一个居中容器、再各自配字号和间距，等于把这套系统里已经调好的字阶与 gap 关系重写一遍，
  而重写出来的必然是通用 Tailwind。
- **补抄进来之后还要照上游的排法用，只挑零件不照排法照样是自制界面。** 三处定型的排法：
  **表**是 `CardFrame` 裹一张 `Table variant="card"`（选中的行给 `data-state="selected"`，
  悬停与选中的底色组件自己会画）——裸 `<Table>` 是一堆靠发丝线切开的行直接坐在画布上，
  那正是后台的长相；**卡片是「几件东西此刻各自怎么样」的板**（任务台那三张），三张同时
  在场才叫板，要点开才知道哪一栏出了事就等于没有板，所以它不承担切换、整块不可点，一栏
  自己的动作（「现在跑一次」）和它排查用的那一半（`Collapsible` 里的全部输出与更早的
  几次）都放在自己那张卡片里；**要让整块可点，只能是真链接**（名单上那三十
  个人、数据页那几百行）：链接自己铺一层 `after:inset-0` 盖住整块，选中态走
  `border-info/40 ring-1 ring-info/30`；挂 `onClick` 的块一律不行——那是只认鼠标左键的
  假按钮，中键、右键、Tab、回车一样都不成立，而这四件事没有任何检查会报，而且可点的块里
  再套一个按钮，本身就是挑错了形状——**要在整块可点的东西上再加一个动作，就把它放到块外面**
  （名单挑人时整份名单往右让出的那一列复选框），两件事各有各的命中区，谁也不必猜点在哪儿会发生什么；**「几块面属于同一件东西」是
  `Frame` 托盘**（数据页一个人的每一段），面嵌在托盘里，
  托盘色从面与面之间透出来说「还在同一份里」——排成几张各自浮在画布上的 `Card` 的话，
  它们和页面上任何一块别的面就是同一个层级，而它们不是；**打开来读完就关的一份东西是
  `Sheet`**（数据页点开的那个人），从右边盖在表上，读完关掉，表在底下原样待着——排成
  并排的第二栏的话，表得永久让出 28rem，而页面抬头那一行的右边就空出一块谁也够不着的
  地方。第二栏只留给要和左边来回切的场合（工作台那条详情面板）；**一次批量操作的收口是浮在
  内容下沿的一小块 `Toolbar`**（名单上那块「已选 N 人 · 清空 · 导出 N 人」），挑上第一个人
  才浮出来、退出就没有——排成又一条吸顶的横杠的话，顶上那一叠（顶栏、查询带、名单表头）就有
  了第四层，而它讲的根本不是「这份名单是什么」；何况空着的时候，那条横杠上三个词一个也按不动。
- **控件的形状要说出它背后那件事的基数。** 一维之内能同时选好几项就是一组复选框，
  只能选一个就是一组单选加一枚「不限」（左栏那七维，`-lib/filters.ts` 的 `multi`）——
  两类长成一模一样的按钮，「再点一个」是加上去还是换掉刚才那个就只能点下去才知道。
  同理，**表达状态的东西不要做成命令**：选中态交给 `Checkbox` / `Radio` / `Toggle`
  自己带（它们各自有 `data-checked` / `data-pressed`），拿 `Button` 加一层底色去仿，
  屏幕上看着一样，读屏念出来的却是一颗没有开关的按钮，而这件事没有任何检查会报。
  菜单里的开关同理（chip 那枚「启用」走 `MenuCheckboxItem variant="switch"`）：一行
  在两句话之间换文案的字，得读完才知道此刻是哪一态。**反过来也一样：一次动作不要做成
  状态。** 「挑人导出」按下去这份名单一个人不少，它是按钮；它左边的「仅岗位或序列」
  按下去会让人从名单上消失，那才是 `Toggle`——两者同款同尺寸并排，等于宣称它们是
  一类，所以中间还隔一条 `Separator`：左边讲「这份名单是什么」，右边是「对这份名单
  做点什么」。
- **也不要把 coss 的组件撑变形。** 用它自己的尺码 API（`size`、`variant`），不要拿
  `h-14`、`rounded-xl`、`[&_input]:h-14` 这类任意值去改一个控件的比例——撑一次，内边距、
  `before:` 顶光边的圆角、图标与文字的间距就全都不再是那套算好的关系，凑近看是「仿的 coss」，
  而这正是山寨感的来源。布局宽度（`max-w-*`）可以覆盖，那是组件外面的事；组件内部的比例一律不动。
  **位置和显隐也是外面的事，就写在外面那一层上**：配方里已经有 `relative` 和 `h-8 sm:h-7`，
  从外面再叠 `fixed`、`sr-only`，同一个属性就有了两个同权重的出处，谁生效取决于生成的 CSS 里谁
  排在后面——赌赢了看不出来，赌输了是一块凭空占位的透明区域。要位置就拿一个只管位置的元素
  包住它（`__root.tsx` 里那条跳过导航）。
- **线只画在有结构含义的地方。** 这套设计靠线分层、不靠投影，所以线本身就是稀缺的：页框两根、
  顶栏一根、左右两栏各一根，加上每张卡片自己的边——一屏已经不少。分组该用间距，`Separator`
  只留给「两侧是两类东西」的地方（详情面板里事实网格与时间轴之间那条）。
- **不自造色板、不写魔法像素值。** 颜色一律用语义令牌（`bg-card` / `text-muted-foreground`
  / `border-border` …），尺度用 `styles.css` 里已经定下来的那几档——宽度只有那几个
  `--container-*`，字阶只有四档、`text-*` 里只用得到 `text-sm`（正文）和 `text-xs`（降权的
  附属信息）。每一档为什么是那个数写在令牌旁边；要加一档，先回答现有的哪一档不够用。
- **色相各有一个主人，全站唯一**：绿（`success`）= 受控字段命中，蓝（`info`）= 选中，
  amber（`warning`）= 要留意但不是错误的状态（任务的「等待中」、`Alert` 的 warning 档）。品牌标记不占色相——
  coss 是中性色系统，`--primary` 是「最重的前景色」，不是一个品牌色。要给任何东西发第四个
  色相，先回答它为什么不会被读成上面三件事之一。
  - **管理页上的运行状态不借这三块填色**：成功 / 正在跑 / 中断 / 失败走
    `routes/-components/status-badge.tsx`——`Badge variant="outline"` 加一枚 `size-1.5` 的点，
    coss 自己的状态列就是这么排的。一枚小点读起来是状态灯，不会和检索那三件事撞；四种
    填色徽章则是四个抢眼的色块，而那一页的主角是日志。**状态只报异常**：常态（成功、
    已派生）不发徽章，一栏里十几块一模一样的绿等于没有信息。
- **强度（必须 / 加分 / 排除）写在符号上，不写在颜色上。** 「必须」不带符号——它是默认，
  而默认不该有标记。
- **证据强度点阵是唯一需要学习的编码**，证据行、详情面板、时间轴、图例必须共用
  `#/components/evidence.tsx` 的 `Dot` 与 `BAND_FILL`，不要各画各的。三档靠**填充方式**
  区分而不是三种颜色，所以它在黑白和色弱下仍然成立——加一档或改成纯色相之前先想清楚这一条。
- **画布必须比卡片暗一档**（`--color-canvas` vs `--card`）。coss 这两个令牌在浅色下都是纯白，
  差 0，台阶是这个项目自己指过来的。抹平它，一列候选人就退化成「铺满窗口、靠发丝线切开的
  平面」，也就是后台的长相。
- **不要给汉字造 display 档。** 试过一次：把零态屏正中那行标题放到 44px 加粗，结果整屏
  立刻显得山寨。拉丁 display 字（coss 自己带的 Cal Sans VF 有 `GEOM`/`opsz` 可变轴）放大之后
  字形本身有性格，汉字系统字笔画等粗、字面撑满字身，放大只会读成横幅标语；何况「人才搜索」
  是个品类名，把品类名当商标放大正是山寨货的长相。一屏的重量交给**结构**去扛
  （零态是那块多行的输入面，名单是面与画布的台阶），不要交给字号。
- **同一份数据只画一遍。** 这条最容易在评审里点头、在代码里违反。判断方法：把其中一个表示
  删掉，如果没有信息丢失，那它本来就是重复的。查询台上那排 chip 不属于复述——原话是
  查询的完整表示，chip 是系统读出来的那一份，两者不是同一份数据的两遍：读错的时候，
  差别恰恰长在它们之间。
  详情面板尤其容易犯：人是从名单上那张卡片点进来的，而那张卡片已经逐词给过命中摘要，
  **且给得更细**（它有命中的字段值）。在详情里再画一份更少的摘要，这一栏就白开了——
  它的价值是逐段核对（轨迹条给形状、时间轴给原文），不是把卡片重说一遍。
  同一次**交互**也只回应一遍：整块卡片可点、悬停时整块换底色，里面的姓名就不另加下划线；
  同一条**分界**也只画一遍：头和身子的字重字号已经拉开，中间就不另画一条分隔线。
- **同一个值只许有一个出处，靠注释维持的一致性等于没有一致性。** 「这里要和那边同高 / 同尺码」
  写成注释，改一处忘一处就是静默漂移，而症状（顶部跳一下、零态的按钮变了个样）没人会当成
  bug 报上来。所以详情面板骨架和真身共用 `HEAD`，名单骨架和卡片共用 `PAD`，常驻那一叠的
  高度只有 `--header-height` / `--deck-height` / `--chrome-height`（读者都列在它们旁边），
  各屏共用根路由上那一个 `AppHeader`，
  三个管理页（`/data`、`/skills`、`/tasks`）共用 `AdminPage` 的版心与标题。
- **一个决定只留一个执行点，而且必须是看得见的那一个。** 「一个人都没有时不报数」写在
  调用点——空态那一支不渲染表头——而不是写成 `ResultHeader` 里一条让它恒为 `null` 的守卫。
  藏进守卫的话，调用点看起来画了一个表头而屏幕上什么都没有，读代码的人得先在脑子里
  跑一遍守卫才知道结果。**判据**：把守卫删掉，如果任何真实输入下的画面都不变，那它就是死的。
- **注释给的理由必须是真的，而且只解释在场的东西。** 「为了稳定身份」只能写在真的进了
  某个 effect 依赖数组的值上（`NO_OUTCOME` 是，分面不是）；理由套错地方，下一个人会以为
  凡是空值都得提成常量。同样不要解释一个不存在的做法——「这里没有用 X」是变更说明，
  不是代码注释，它会让人以为 X 曾经在这儿。
- **不要做「覆盖度 n/m」这类指示器。** 必须词是 AND 语义，它恒等于满值；要表达的是每个词
  命中得有多强。
- **改问题和改看法不共用一块面。** 「记录还是视图」是这个产品最要紧的一条界线：改那句话
  或那几枚 chip 会**派生一条新的查询记录**，改筛选只动 URL（见 `routes/-lib/commit.ts`）。
  屏幕上由**位置**说出来——查询台在名单上方、筛选在名单左边——挤进同一块面时，这条界线
  就只剩 10px 的间距在表示，等于没表示。**判据**：如果屏幕上出现了第二个自由文本框来补救
  第一个表示的不足，缺的是第一层，不是第二个框。
- **筛选栏的形状只随查询变，不随筛选变**（口径在 `rank.ts` 的 `facetRows`）。**判据**：
  如果点一个筛选会让另一个筛选的选项消失，缺的是「有哪些行」和「每行几个人」这两个口径
  的分野，不是一条「不要点太多」的提示。一维之内多选是「或」、维度之间是「与」，这个差别
  在 `dimensions.ts` 里声明一次（`isMulti`），左栏画出来的七维长得一模一样。
- **左筛选、右详情，中间是那条唯一的名单列。** 两侧形状一致（吸顶、限高、自己滚、靠一条
  发丝线分层，不靠投影），因为它们地位一致：都不改「问的是什么」，只改看到的是哪一部分、
  哪一个人。查询带不在这三栏之内，它是这一页的抬头，吸在顶栏下沿、和三栏一起常驻。
  - **常驻的东西定高。** 顶栏、查询带是三栏与名单卡片让位的依据（`--chrome-height`），
    所以它们的高度不许跟着内容变：查询带里那句原话长了就截断，改写框和错误提示从带子里
    浮出来、不推开布局。跟着内容长高的常驻带会把下面每一栏推到自己算好的位置之外，而
    `tsc` 和测试全绿——只有滚一下才看得见栏顶被压掉一截。
- **自己滚的面一律 `ScrollArea`，不手写 `overflow-y-auto`。** 原生滚动条一出现就占宽度：
  内容当场被挤窄一格，而这几栏的宽度是版心算式里的项，不该由「这一屏够不够长」说了算；
  它画出来的那条常驻灰带也比全站任何一条发丝线都重。`ScrollArea` 的拇指浮在内容上、不滚不显。
  吸顶的头在里面时别开 `scrollFade`，遮罩会把它一起蒙掉。**内容能无限长的面，高度由版面
  给、不由内容给**（任务日志是 `--task-log-height`，两侧的栏是视口减顶栏）：跟着内容长的
  「自己滚的面」根本不会滚，它只是把下面的东西推到看不见的地方，而屏幕上没有任何迹象。
  **页面这一层**不套 `ScrollArea`
  （那是浏览器的滚动条），靠 `styles.css` 里 `html` 的 `scrollbar-gutter: stable` 把那一格
  宽度永远留住——否则不滚的页面切到要滚的页面时，整页连同页框那两根线横着跳一格。
- **图例必须和它解释的那些点同屏**——别把它挪进任何「空着才显示」的位置（详情面板的空态是
  最诱人的那一个）：那类位置空着的时候，正意味着屏幕上一颗点都没有，于是**只在没有点的
  时候才解释那些点**，而这个错误全绿。
- **屏幕上只留会变的东西，说明书写进代码注释。** 「这一格是干什么的」「这一页怎么用」
  「换个写法再试试」这类话一个字都不会变，而它们全长成同一种 12px 的灰字：几句下来，
  真正会变的那一句（还剩多少段待派生、上一次几点跑的、库里多少人）就淹在里面了。
  **判据**：把这句话删掉，如果读者在任何一次真实使用里都不会因此少知道一个**会变的事实**，
  它就是说明书。空态是唯一的例外，那里的一句话是出路（「先在命令行跑 bun run sync」），
  而且只在真的没有出路可循时才写——标题已经说清的，下面不再补一句。任务的节奏
  （「每天 03:00」「命令行 bun run sync」）也是说明书：它一个字都不会变。
- **会变的数长在说它的那块东西上，不在页面抬头。** 管理页的标题底下不挂概述——那一句
  写的是「有多少人、还剩多少段、上次整理什么时候」，而下面的表和卡片本来就在说同一批数，
  抬头那份还得自己再算一次（`/skills` 那句三个数里有两个是把下面那张表重数了一遍）。
  数归它该待的地方：表脚说这张表和库的关系（`/data`：列着 200 行，而库里有 5000 人），
  卡片说自己那一栏此刻有多少。
- **管理页给的是此刻的状态，不是终端回放。** 这几页也是产品，读它的人问的是「语料现在
  怎么样」，不是「那个进程往标准输出打了什么」。所以卡片上那几行数**问库**（`corpusCounts`：
  多少人、公司内与入职前各多少段、还剩多少没派生、能力词多少个并了多少），过程那上千行
  收在折叠里，要排查才展开。**不要把运行时说过的数抄进库里当界面的出处**——「人群 20 人」
  是那一刻的快照，下一次同步之后卡片还照着它报数，就成了屏幕上一个没人维护的旧值，
  而这个数库里现成就有。
- **提示分两档：页面级的事件用 `Alert`，查询上的注解用一行小字。** 「没能提交」是前者，
  「这个词太宽，已自动停用」是后者。给一句注解发一整块满宽 amber，它就成了整屏第二重的东西。
- **按压反馈和触控目标不要自己写。** coss 的每个控件都自带 `data-pressed` / `:active` 态和
  `pointer-coarse:after:min-h-11`——只在粗指针设备上撑开，桌面上不会造出一圈看不见的命中区。
  想用 `after:-inset-*` 手撑命中区之前先看一眼：伪元素带上 `pointer-events-none` 就一个点击
  都接不到，而这件事**没有任何检查会报**，只有拿手指去点才发现。
- **层级只有 `styles.css` 里那四档命名 z 值**（`z-raise` / `z-stick` / `z-frame` / `z-escape`），
  代码里不许出现 `z-10` 这种裸数字，也不要靠给弹层补更大的数字——外壳根节点的 `isolate`
  已经从结构上解决了这件事，理由写在 `__root.tsx`。
- **动效是列得完的，加一处之前先删一处。** 全站只有一个关键帧动画：换人时详情栏的
  `settle` 淡入（工作台那块面板，以及数据页右边那一栏——同一件事，所以是同一个动画，
  不是第二个）。除它之外只有两处 `transition`，两处都是**布局或状态变化的补间**，不是入场
  演出：详情面板收展时的 `transition-[width]`（route.tsx），和名单卡片选中 / 悬停时的
  `transition-[border-color,background-color]`（result-list.tsx）。组件自带的那些（`Button`
  的 `transition-shadow`、`Collapsible` 的收展、弹层的补间）不算这一份清单里的，它们跟着组件一起来。
  同一件事也不要画第二遍——选中态已经由边框、环、`aria-current` 和推开的详情面板说了四遍，
  再加一根需要 `offsetTop` + `ResizeObserver` 量出来的滑轨，只是第五遍。
- **路由文件只接线，不产状态。** `createFileRoute` 那一层负责 loader、把数据喂给组件、
  排版；派生状态归 `-lib/` 的 hook（`useNavPhase` / `useInterpretation` / `useCommit` /
  `useKeyboardFlow`）。判据是**能不能测**：这里错一个布尔量不会有任何东西报错，
  只会在扫名单时每按一下就清空一次列表。所以 `navPhase` 是纯函数、`useNavPhase` 只负责
  从路由取两头，两种 pending 的每一种组合都在 `tests/nav-phase.test.ts` 里测。

## 查询、视图与计数

- **查询是一条记录，视图是几个 URL 参数——这两样东西不许混。** 「我要找什么人」完整落成
  `SearchSpec`（证据要求、结构化范围、未生效提示）并保存在 `search_turn`，地址上只出现它的 id；「我怎么看这批人」
  （各个筛选维度、翻页数）留在 query string，当前员工由 `/p/:empId` 子路由表达。范围与筛选
  是**同一批维度的两种生命周期**，所以那批条件只声明一次：`SearchScope` 是那一份，
  `SearchFilters` 是它加上证据强度，`View` 是筛选加上翻页数，谁也不重写一遍别人的字段。
  收窄它们的入口同样只有一个（`params.ts` 的 `parsePopulation`），「这份条件收窄人群了吗」
  也只有一处（`narrowsPopulation`，问的是取值不是键）；分家写两遍就是「一维要改十几处」的来处。判据是**值不值得留存**：前者要能重新
  理解、能出现在最近搜索里、分享出去仍是同一个问题（复现的是条件，不是名单——
  名单跟着语料和时间走），后者一次性。
  - **手上正在做的一份活，两样都不是。** 挑出来准备导出的那几个人只活在内存里
    （`-lib/picks.ts`）：三十个工号写进 query string，每勾一下就往历史栈里压一条，
    后退键当场作废；而它也不值得留存——换一条查询就该从头挑，因为上一批是按另一套条件
    挑的。判据仍是那一条「值不值得留存」，只是这一次答案是「连 URL 都不值得」。
  - 写查询只有 `routes/-lib/commit.ts` 的 `useCommit` 一个入口（不可重入 + 失败可见），
    写视图只有工作台路由里那个 `updateView`。把它们并成一个函数就是这条边界消失的方式：
    改一个筛选和换一个问题在代码里长得一模一样，而它们的代价差着几个数量级。
  - 条件从哪来由 `spec.ts` 的 `QueryInput` 判别联合说话，两支：`sentence` 过查询理解，
    `spec` 直接落地。界面编辑必须提交完整 spec，
    不能只传 chips 再靠 URL 或父记录补齐其余含义。
  - **一句话就是一份完整的条件。** 改写那句话整份重来，不在上一版条件后面追加——追加的话，
    从句子里删掉的词会从上一版活着回来，而屏幕上那句话读起来完全正常。`spec` 记录（只调
    了几枚 chip）把原话原样带下来：问的是什么没变，只是读法调了一下。于是每条记录自己的
    `raw_text` 就已经是它的门面，最近搜索直接读它、不回溯根记录——回溯的话，改写之后拿到的
    是旧话配新条件，屏幕上写着一件事、点进去搜的是另一件。
  - **记录不可变。** 改条件一律派生新记录并 push 导航，于是后退键天然是撤销。`search_turn`
    只 INSERT，唯一的 UPDATE 是把 `spec` 从 null 补成理解结果，且必须带 `where spec is null`。
  - **不要把条件写回 URL。** 那样做原话就丢了（于是改写只剩增删几枚条件），理解结果被冻结成
    事实（模型改好了惠及不到任何一条已有链接），而「模型判成必须、人改成加分」这类修正——这个
    产品唯一能自己产出的模型评估数据——每次导航扔一次。
- **换人用 `replace: true`**（↑↓ 与点击）。换人是「在看哪一个」，不是一次导航；不 replace 的话
  扫过三十个人就往历史栈里压三十条，后退键和手机侧滑都失效。只有换查询、改筛选才 push。
- **理解不许挡在导航前面。** 整句提交只落一条记录就导航，模型那一跳由工作台自己补
  （`spec === null` 时触发）。模型单次可以从一秒跳到几十秒，让它挡在零态和工作台之间，人看到的
  是首页卡住了；转圈要发生在结果将要出现的地方，不是发生在按下去的那个按钮上。
- **翻页的 pending 和换查询的 pending 不是一回事。** 换查询要塌成骨架屏（旧结果已经不成立），
  翻页必须把已经看到的人留在原地，只让按钮转圈。而**换人不算换查询**——路径也变了，只比
  pathname 会让扫名单的每一下都把列表清空。
- **计数只有一个单位：人。** 筛选栏里每一项后面的数是「点了它还剩多少人」，名单表头那个数是
  截断之前的总人数，页脚那句「已显示 X 人，共 Y 人」里的 X 才是这一页有多长。数段数或数页大小
  都会让同一屏出现两个互相拆台的数。**新增任何数字前先回答它的单位是什么。** 唯一的例外是
  筛选栏那几个数控件的：「清除 N 项」「更多 N 项」紧挨着「项」字，窄屏收起时贴在「筛选」
  二字后面的那个数也是项数——它们数的是控件，不是人。
- **列表不许静默截断。** 只要 `total` 超过一页，页脚必须同时说出「已显示多少」「一共多少」
  「按什么排的」。翻不动时不留一个按不动的按钮，改说原因：「已经是全部了」和「到上限了」是两件
  事，后者要给出路。
- **显示出来的数，必须就是参与排序的那个数。** 显示某一段的月数而排序用累计月数，会让两个名次
  不同的人长得一模一样——一个把「可解释」写进产品原则的工具，最不能容忍的就是这个。
- **分数不上屏；名次由位置说，不另写一份。** 分数没有刻度让人校准「1.35 算高还是低」。名次
  有刻度，但那把尺子就是名单本身——第几个就在第几行，所以卡片上不标序号、详情面板里也不写
  「第 N 位，共 M 人」。名单表头那句「按相关度排序」是这件事唯一需要说出口的部分：它管整份
  名单，代价只有一次。
- **对不上的数一个都不给。** 一个入口旁边挂人数（「算法 128」）就是这么破的：那个 128 是
  「有过算法序列经历的人数」，而点下去是一次检索，它还会从岗位、部门、简历原文命中另一批人，
  两个数必然对不上。全站计数只有「人」这一个单位，且必须同一次检索同一口径。
- **候选事实过多是一种结果，不是一次失败。** 事实超过 `FACT_MAX` 时走
  `s/$turnId/-lib/empty-state.ts` 那套引导，不截断、不抛异常。不能拿 `WIDE_SHARE` 的人数
  覆盖率代替：一把尺管内存载荷，一把尺管条件区分度，单位不同。
- **空态的成因由检索层给，界面只负责说话。** 零结果的那几种成因长得一模一样（都是零行），
  出路却正好相反——「你自己把条件全停用了」和「真的没有这样的人」完全相反，而说错了没有
  任何断言会红。所以「为什么是空的」是这次检索的产物（`search/empty.ts` 的 `EmptyReason`，
  挂在 `SearchOutcome.empty` 上，取数超限也是它的一种取值）：那一侧手里有事实、AND 判定和
  筛选，成因是它顺手就知道的。界面上那张表（`s/$turnId/-lib/empty-state.ts`）只把成因翻译成
  一句话和一条一键出路，而且是**穷尽的** `Record`——多一种成因少写一句话，`tsc` 当场就红。
  在界面上拿几个二手计数反推，等于把服务端刚做过的判断再做一遍，两份推理迟早分叉。
- **零态那几句示例点下去是填进输入框，不是直接搜。** 它们是句式的样板，四条各教一件
  placeholder 教不了的事（一句里放多个条件 / 条件之间是 AND /「最好」是加分档 /「或者」是一条
  条件的两个取值），要找的人几乎不会正好是其中哪一句，填进去才改得动。四条是上限：再多就
  从「样板」变成「目录」，人会开始挑而不是改。每一句都必须走完整条路（句子 → 理解 → 检索）
  跑一遍，在库里搜得到人——第一次点它得到一份空名单，是这个工具能给的最差的第一印象，
  而原因不在用户身上。**没有任何断言看得住这条**——句子是手写的，语料是会换的，换完只有
  人眼能发现。

## 全绿的陷阱

下面几条违反之后 `tsc` / `biome` / `build` 全过，只有人眼或用户能发现。

- **加一维要改几处——答案必须是一处**（`search/dimensions.ts`，取值、身份、中文名、选项
  文案、清洗规则、排序规则都写在同一段里）。今天唯一另外还要手写一笔的是 `search.ts` 的
  `DIM_COLUMN`：`Record<DimKey, SQL>` 逼着它穷尽，漏一维当场红。除此之外**任何**「加一维
  顺手也要改的地方」都是一处该被遍历取代的散写——散着写的时候漏一处不报错，只是算错。
- **宽度令牌必须定义在 `--container-*` 命名空间下。** 写成 `--spacing-page` 的话
  `max-w-page` 会**静默**变成一个不存在的类，构建不报错。
- **要盖掉某个抄来的组件自带的宽度时，必须写变量形式** `max-w-(--container-page)`。
  `cn` 里的 tailwind-merge 只认得这一种；类名形式它当成两个无关的类，谁赢由样式表里的
  先后决定（`.max-w-sm` 排在 `.max-w-page` 后面），而两种写法在编辑器里长得一样。
- **设计系统组件不是 HTML 标签。** 它们常把一件事拆成几份互不重叠的契约，只喂一份照样能渲染出
  一个「看着完整」的元素——比如只给 `children` 不给 `items`，选中后会把 `internal`、`12` 这种
  URL 原始值直接显示出来。写之前先看 coss 的组件文档或 `src/components/ui/` 里那份源码。
- **详情面板画哪一份内容由路由决定，不由外壳里的三元表达式决定。** 各种状态各自属于一个路由，
  外壳只无条件渲染 `<Outlet />`。
- **分面必须跟着这一次检索走**，和名次出自同一份事实，所以没有独立的 `fetchFacets`。
- **「证据要求」和其余各维一样是服务端筛选。** 放到客户端它就只能筛已经翻出来的那几页，而按钮上
  其余各维数的是全部命中的人——同一列控件两种口径。

## 提交前

```bash
bun run verify   # check + typecheck + test + build
```

`bun run test` 需要一个可连的本地 Postgres（读 `.env.local` 的 `DATABASE_URL`）。检索语义有集成测试
（`tests/search.test.ts`）：它在临时 schema 上建一份合成夹具跑真 SQL，建表语句由 `schema.ts` 现场
推导，不手抄；三个模型端点由进程内的假端点提供（字符袋向量，相关度可以手算），它测的是机制，
不是语义质量——语义质量归 `bun run eval` 和真模型。改 AND 语义、打分公式、阈值或路径判定
之前先看它。抽取和整理的提示词各有一把同样的尺子（`bun run eval:extract`、`bun run eval:review`，
用例在 `evals/extract/`、`evals/review/`）：走派生和整理同一条路，量当前提示词配当前模型的召回、
精确、限定语、有损合并、归属和起名。改提示词先跑它，不然分不清是变好还是变坏；合成用例进仓库，
真实用例（真人的简历段、库里圈出的词组）留在本机。

整条语料链路也有一个（`tests/import.test.ts`）：读仓库自带的合成样例跑一次完整导入，
断言人数、说法、边和序列。链路上每一段单独看都对得上、连起来却可能对不上——暂存表的列
顺序、`unnest` 每一列的类型、发布之后序列有没有跟上——而这类错在屏幕上的表现是「搜什么
都搜不到」。改暂存、发布或说法规划之前先看它。
