# talent-search 开发约定

运行方式、模块职责与数据来源见 [README.md](README.md)，产品范围见 [docs/PROJECT.md](docs/PROJECT.md)。
本文件只保留禁令、跨文件不变量，以及自动检查无法覆盖的验收要求。
局部实现的约束写在所属模块；注释描述当前行为和必要原因，不记录修改经过或替代方案辩护。

## 数据与语料

- 真实员工数据不进版本库。源适配器只读数据源，同步只写 Postgres；parquet、导出的 CSV、
  查询结果和含真实姓名工号的样例均不得进入代码或文档。夹具和仓库用例只能使用合成数据。
- 测试运行前必须把 `TALENT_SOURCE` 固定为合成样例，使用临时 schema 和假模型端点。
  `.env.local` 中的数据源可能是真实人事数据，不能直接用于测试或验收夹具。
- 数据源与管线之间只有 `src/corpus/contract.ts` 一份契约。换数据源就必须重写的逻辑归适配器，
  与来源无关的切段、日期校验、合并与派生归管线。管线不含具体源字段名或按源名称分支。
- 适配器默认私有：`.gitignore` 只放行 `csv-dir` 和样例。表名、字典码也属于内部信息。
  按名字加载适配器使用 Vite 支持的变量动态 import：`./` 开头、一级文件名、带扩展名；不建注册表。
- 公开表结构唯一来源是 `src/db/schema.ts`。改结构运行 `bun run db:push`，不生成迁移文件。
  同步仅通过 `LIKE ... INCLUDING GENERATED` 创建事务内暂存表；pg-boss 自管 `pgboss` schema。
- 修改 check 表达式时同时修改约束名：`db:push` 不比较同名 check 的表达式。
- 语料只有同步、派生、整理三个写者，由 `src/corpus/session.ts` 的会话级 advisory lock 串行化。
  后台任务拿不到锁就等待下一轮，命令行同步排队等待。独占连接收尾失败时销毁，不能还回池子。
- 段按数据库生成的内容键识别；应用不重复计算内容键。派生版本由提示词、模型和嵌入空间摘要决定，
  不另设重跑开关。派生按批提交，模型调用不得发生在数据库事务内。
- 跨语句检索读取使用 `withCorpusSnapshot` 的 repeatable read 快照。
  `embedding_space` 只在换空间时更新；它的行版本变化使查询准入失效。
- `phrase` 的文本和向量不改，没有边指向的行在派生落库时删除；删除段只级联删除边；
  换嵌入空间时清空说法及依赖数据。`skill_term` 与 `phrase_gloss` 跨数据源和嵌入空间保留。
  整理只写词表和释义，不改任何人身上的说法与边；写释义的同时清那条说法的分数缓存。
  判定读「说法：释义」；技能、做过的事、岗位名、序列名写释义，原文和部门路径不写。
- 派生物记得住自己出自哪一版标准：段上是 `derived_identity`（模型、抽取与对齐提示词），
  释义上是 `guide_identity`（`GLOSS_GUIDE` 与回答形状）。标准一改，旧的那些自动重来。
  词表不记：每个词七天到期重判，改了一周内自然换完。一批数据必须出自同一份标准——
  跨人、跨说法比名次时，两边不是一个口径就比不出高下。
- 缓存按输入身份保存第一份成功落库的结果，并发调用者消费相同的已保存值。
  模型调用和缓存写入不依赖语料写者锁；无效模型回答不得进入缓存。
- 检索 SQL 手写，集中在 `search/phrases.ts` 和 `search/search.ts`。
  Drizzle 用于表结构及按主键取实体，不引入第二套检索查询构建方式。
- 日期倒置、缺开始日、生效日在未来的源记录必须报告并拒绝，不猜日期或钳制错误区间。
  任务输出进入 `task_run.log`，命令行和 `/tasks` 读取同一份记录。
- 每个持久化字段和 `org_meta` 键必须有实际读者。去重键、回填入口、匹配别名等工作值算完即弃。
  不添加预留字段、无人调用的导出、无外部使用者的类型导出或未经运行验证的脚本。

## 检索与模型

- 查询的唯一表示是 `Condition[]`，模型输出、RPC、记录和界面共用 `conditionsOf` 的收窄边界。
  条件只有两种：经历主张（做过什么、在哪、在哪一档、什么时候、累计多久，各项说的是
  **同一段经历**）和人的条件（职级、学历、招聘渠道、学校）。条件之间 AND、同一个人；
  一项内的取值 OR 且同权。修饰语挂在哪条主张上由模型在读句子时决定，实现里只有这
  一种绑定口径。命令行文本语法只属于 `query-syntax.ts` 和验收工具。
- 维度的取值解析由 `search/dimensions.ts` 定义，条件里的词表取值走同一份 `parse`。
  界面桶不是数据值域；上限放在收窄处，不在模型 schema 重复限制。
- 主张上的 `minMonths` 是累计口径，筛选栏的「经历时长」是单段口径：两者不是同一件事，
  各有各的标签，不合并。
- 搜索词表达要找的经历，不标注用户原话或模型补词的来源。词经过 `termOf` 后不再改写字面。
  无法用现有维度表达的条件不写入查询；可表达的偏好使用 boost。
- 公司和学校名称只走精确文本条件，不进向量。SQL 的 ILIKE 输入必须经过 `escapeLike`。
- `search.ts` 只产出事实；打分、AND 判定、排序、分面属于 `rank.ts`。
  SQL 仅允许同一主张、同一经历段内按强度去重，不计算人员分数。
- 所有匹配系数集中在 `search/weights.ts`。不为具体词、查询或路由设置特殊阈值。
  排除的主张采用更严格的相关度阈值，否决经历段，且不带视图筛选。
- 语料和查询共用 `server/embed.ts` 的配置。模型、权重、归一化或端点实现变化时更新
  `EMBED_SPACE_ID`；维数只由 schema 的 `EMBED_DIM` 定义。查询核对空间元数据和 fresh canary，
  不一致拒绝检索。重排行为变化时更新 `RERANK_SPACE_ID`。
- 一段经历有哪些说法只有 `corpus/route-texts.ts` 一份拼法，派生和测试夹具必须共用。
  自述只有一种读法：读过的段只有抽取的说法，没读过的段才有整段原文。
- 判断属于提示词，阈值和权重属于代码，词频和词表取值来自语料。
  模型只从语料词表选择词表维的取值；`toSpec` 丢弃不合规取值，不将其改成经历词。
- 模型端点只由 `server/llm.ts`、`embed.ts`、`rerank.ts`、`chat.ts` 调用，超时与重试
  共用 `server/endpoint.ts`。适配层不判断能力词、归属、序列等领域含义。
- 抽取产物进入 `phrase` / `experience_phrase`，与登记的三路共用召回和打分机制。
  `skill`、`did` 与自述字段同档；参与方式存在边上，仅用于证据展示，不进入向量。
- 排名的两项依据不相乘：可信度按档比，深度只在档内比；`weights.ts` 里没有任何一个数
  用来守住档位。召回按余弦取前 `RECALL_TOP` 条，成本与语料大小无关；太宽只按命中人数占比量。
- 能力词保留最细粒度。同义写法归成标准词并迁移边；宽细关系只记 parent，细词保留在经历上。
  标准词和判定方命名的父词都参与圈组，人数按它及其后代的人群计算。标准写法由同义组人数决定。
  词表拒绝别名链、归属于别名和归属环。纯规则集中在 `corpus/vocabulary-rules.ts`，
  生产让判定生效的那一步和验收共用；验收检查组里全部词的作答覆盖，并按最终标准词、归属计分。
- 整理的外部接口只取走待判的组和提交判定，只能写 `review_group` 的判定那一行，
  不能写词表、释义或经历边。两种组（归并、释义）共用一条队列和一个判定方；自带模型和
  外部判定方共用各自的 GUIDE 与收窄；接口保存原话，生效时收窄。
  一个词同时只在一组中，一组只接受第一份判定；不投票仲裁多份判定、不设置判定租约。
- `seq_l1..3` 只保存登记值；推断序列单独存列，仅用于筛选，不作为受控字段证据。
  推断必须是序列树上完整的一对，或者两级均空。
- 查询理解只发送用户输入和几维的词表；嵌入、抽取、重排会发送经历内容，文档必须说明部署数据边界。
  姓名、工号不发送至模型端点；密钥只通过环境配置。
- 查询理解、嵌入、重排未配置或失败时抛出错误，不降级为空名单。
  抽取未配置时派生报告并跳过；单段无效回答报告后跳过且不缓存。
  抽取返回值必须区分“模型未能作答”和“成功但没有说法”，验收不能把失败当空结果通过。
- 模型输出是不可信输入，schema 校验后仍须领域收窄。提示词描述含义、判断规则和完整例子；
  不包含实现论证或与收窄重复的数量限制。维度提示词手写，新增模型能力必须单独验收。
- 查询理解仅用 POST，输入输出均有硬上限；公开部署的限流放在网关层。

## 分层与服务端边界

- `corpus/` 负责构造语料，`search/` 负责检索，两者互不 import，通过 `db/schema.ts` 共享结构。
  `server/` 承担 RPC、查询记录、任务排班和模型端点；与界面无关的跨层纯函数放 `lib/`。
- `db/index.ts` 只出连接，表定义从 `db/schema.ts` 导入。
  `search/result.ts` 是页面可读取的结果契约，带数据库的模块只能向页面提供类型。
- 碰连接或密钥的模块自行声明 `import "@tanstack/react-start/server-only"`。
  `db/schema.ts` 供 drizzle-kit 以 CJS 加载，不加此标记；纯规则模块不依赖端点或连接。
- 页面不能判断服务器才知道的状态，如任务锁归属。服务端提供结论，页面负责文案。
  URL 的 `validateView` 和 RPC 的 `sanitizeFilters` 分别保护各自边界，不互相替代。
- 目录按实际使用者划分：

| 目录 | 内容 |
| --- | --- |
| `components/ui/` | coss 通用组件源码 |
| `components/` | 跨屏使用的产品展示组件 |
| `routes/-components/`、`routes/-lib/` | 多条路由共用的外壳组件及逻辑 |
| 路由内 `-components/`、`-lib/` | 仅该路由使用的组件及逻辑 |

- `-` 前缀表示路由私有；不同路由不互相导入私有模块。使用者变化时移动到对应共同祖先。
  路由文件负责 loader、接线和布局，交互及派生状态放所属 hook 或纯函数。

## 界面验收

- 控件和面使用 `components/ui/`，不手写 border、shadow、rounded 组合模拟组件。
  升级或补组件前读取 `src/components/ui/NOTICE.md`，保留上游许可和来源。
- 使用组件的组合方式与 size / variant API，不覆盖内部高度、圆角和间距比例。
  宽度、位置、显隐属于外层布局；每块面的背景、状态和触控目标交给组件。
- 表使用 `CardFrame` + `Table variant="card"`；空态用 `Empty`，输入与提交用 `InputGroup`，
  提示用 `Alert`，同一对象的多块面使用 `Frame`。一次阅读使用 `Sheet`，两侧反复对照才使用并列栏。
  批量操作用选择非空时出现的 `Toolbar`；状态看板同时显示各任务及各自的动作与运行记录。
- 整块可点击必须是真链接，支持中键、右键、键盘导航。链接覆盖层内不嵌套其他动作，选择框放块外。
  铺满整块的覆盖层只用在卡片这类 div 上；表里通往详情的是单元格内的文字链接，
  不铺覆盖层也不改单元格内边距（`<a>` 包不住 `<tr>`，表格行当不了绝对定位的包含块）。
  选中态使用组件状态 API、边框和环，不增加重复的选中装饰。
- 单选使用 Radio，多选使用 Checkbox，一次动作使用 Button。
  菜单开关使用 `MenuCheckboxItem`，不能通过两句交替文案代替状态。
- 颜色、宽度、字阶与层级使用 `styles.css` 语义令牌；不引入自制色板、裸 z 数字或魔法像素。
  宽度令牌属于 `--container-*`；覆盖组件宽度时使用 `max-w-(--container-page)` 等变量形式，
  确保 tailwind-merge 能识别覆盖关系。
- 绿表示受控字段命中，蓝表示选择，amber 表示需要留意。管理页使用共用状态徽章，常态不重复提示。
  必须 / 加分 / 排除靠符号区分，必须为默认状态。证据点阵共用 `evidence.tsx` 的 Dot 和 BAND_FILL。
- 画布比卡片暗一档；汉字标题使用现有字阶，不新增 display 档。
  相同数据和交互反馈不重复绘制；共享尺寸通过代码或令牌表达，不靠注释同步。
- 页面事件用 Alert，查询条件注解使用行内文字。空态文案必须说明当前问题及可执行的出路，
  标题已说明的内容不重复。管理页展示数据库当前状态；任务的原始输出不做页面内容，按次收在「日志」里，打开才取。
- 动效保持有限：详情切换淡入、面板宽度、名单悬停与选择，以及组件自带过渡。
  新增动效前评估现有动效是否足以表达变化，不增加并行滑轨等重复状态表示。
- coss 复合组件必须提供完整契约，例如 items 与 children；人工检查显示标签、键盘和触控行为。
  详情外壳无条件渲染 Outlet，由子路由决定内容。

## 查询记录、视图与计数

- `SearchSpec` 保存 `Condition[]`，含强度、各项取值和停用状态；`search_turn.raw_text` 保存原始输入。
  条件编辑生成新记录，原始输入用于改写和最近搜索展示。整句改写重新理解，不叠加旧条件。
- 查询通过 `conditionsOf` 收窄；URL / RPC 的视图筛选通过 `sanitizeFilters` 收窄；
  维度的取值、身份与解析共用 `dimensions.ts`。
- 查询写入只走 `routes/-lib/commit.ts`，视图更新走工作台的 `updateView`。
  RPC 的 QueryInput 区分 sentence 和完整 spec，不能用 URL 或父记录补齐局部查询含义。
- 查询记录不可变：新增记录使用 INSERT，仅待理解记录允许带 `where spec is null` 补全一次。
  改查询和筛选 push 导航，浏览器后退可撤销；切换当前人员使用 replace。
- 分面、翻页属于 URL 视图，当前人员属于子路由；导出选择仅保存在内存，换查询清空。
  条件不写入 URL。整句提交先落记录并导航，理解在工作台进行，失败显示错误与重试。
- 换查询显示骨架，翻页保留已有名单，换人不清空名单。骨架、空态与名单共用同一套行结构和
  列宽，由同一层容器给出，不在各自分支里重复实现。
- 分数和名次不重复上屏，名单位置表达顺序；显示的时长必须与排序使用的时长一致。
- 分面、总数和排名来自同一次检索的事实，单位为人。筛选控件的项数明确标“项”，不混用。
  筛选在服务端执行；分面选项集合随查询变化，计数随筛选变化。
- 证据可信度只决定名次先后，不决定去留：只有自述证据的人照样进名单，排在登记证据之后。
  界面上档名与路名不同名——档叫「简历自述」，路叫「简历原文」（只指还没读过的段）。
- 管理页的表在服务端找与翻页，一页多少行、第几页由 `server/paging.ts` 定夺；
  搜索词与页码写进地址，过滤和计数说的是整张表而不是当前页。
- 列表不静默截断，说明已显示数、总数、排序和加载上限。候选事实超限由检索层返回对应空态，
  不截断事实后继续排名，也不拿区分度阈值替代内存载荷上限。
- 空态原因由检索层判定，界面穷尽翻译并提供出路，不用二手计数重新推断。
- 零态示例只填入输入框，最多四条；更换真实语料后人工验收每条示例的完整理解与检索路径。

## 验证

```bash
bun run verify  # 格式、类型、测试、生产构建
```

测试需要可连接且带 pgvector 的 Postgres。`bun run test` 使用 `--parallel` 隔离测试文件，
每个数据库夹具建立临时 schema，模型端点使用进程内合成实现。
纯领域规则测试不启动数据库。测试验证机制，不代表真实模型的语义质量。

改变检索语义时检查 `tests/search.test.ts`；改变同步、派生或空间生命周期时检查
`tests/tasks.test.ts`；模型提示词和模型选择使用 `eval`、`eval:extract`、`eval:review` 验收。
三类验收目录均只提交合成样例，真人经历与真实词组留在本机。
