# talent-search 开发约定

运行方式、模块职责与数据来源见 [README.md](README.md)；产品定位与范围见 `docs/PROJECT.md`。

**这份文件只放代码里放不下的约定。** 三类，没有第四类：

1. **禁令**——它管的代码不存在，所以没有任何地方可以注释它；
2. **跨文件的不变量**——没有任何一个文件是它的主人，写在谁那里都只说了一半；
3. **全绿的陷阱**——违反之后 `tsc` / `biome` / `build` 全过，只有人眼或用户能发现。

「某个文件为什么这么写」一律写在那个文件里，这里不复述。复述出来的那一份不受类型检查、
不受测试保护，改代码的人不会顺手改它，于是它只会烂——而写在文件里的同一批论证会跟着
代码一起改。界面这一章尤其危险：换一次组件库就能让几百行描述性的条款集体失效，
而它们失效的时候没有任何东西会红。**看到一条规则能被移进某个文件的注释里，
就移过去，然后把这里删掉。**

## 数据

- **真实员工数据不进版本库。** ETL 只从配置好的数据源读、只往 Postgres 写。不要把
  parquet、导出的 CSV、查询结果或含真实姓名工号的样例写进仓库或文档。仓库里能出现的
  只有合成数据（`etl/sources/sample/`、`tests/fixture.ts`）。
- **数据源与管线之间只有一份契约**（`etl/contract.py`）。分界线是一句话：**换一套数据
  就得重写的，归适配器；换一套数据也必须成立的，归管线。** 管线里出现任何一个具体源的
  字段名，下一个接数据的人就会照着再写一套「什么算一段经历」。
  - **适配器默认是私有的。** `.gitignore` 用白名单放行 `csv_dir` 与样例，其余一律不进
    仓库——表名和字典码本身就是一家组织的内部信息。规则的方向很重要：新写一个适配器
    **天然**被忽略，「忘了加 ignore」这件事不可能发生。
  - **管线不许为某一个源开分支。** 源之间的差异用契约的取值表达（比如 `segment_key`），
    不用 `if source == ...`。
- **公开表结构的唯一事实源是 `src/db/schema.ts`**，改完跑 `npm run db:push`。Python 不建
  公开表、不改结构，只能从公开表 `LIKE` 出连接私有的暂存表并 INSERT；**不生成迁移文件**
  ——两套改表机制并存就是同一件事两个入口。
- **`embedding_space` 同时是整代语料的换代门闩。** 跨语句读取统一走
  `withCorpusSnapshot`，先持有它的共享锁；ETL 先在连接私有的暂存表完成嵌入，
  发布事务再从这张表起独占锁并一次替换所有公开语料。模型调用不许发生在门闩内，
  否则一次端点延迟就会阻塞全部检索；也不许绕开这两个入口另写整库重灌或多语句读取，
  否则一次结果会混入两代数据，或因各表锁序相反形成死锁。重灌从准备到发布由会话级
  advisory lock 串行化，不能让两代暂存语料竞速决定最后发布谁。
- **drizzle 是 schema 与迁移工具，不是查询层。** 检索的 SQL 一律手写、集中在 `search.ts`：
  召回要 `lateral` 取每个词的前 N 个说法、同段去重要 `distinct on`，查询构建器两样都做不好。查询构建器只用在
  **按主键取实体**那一类（`functions.ts` 取单人时间线）。这条不写下来，下一个人顺手用它
  写一条检索，两种风格就并存了，而没有任何关卡会拦。
- **拒绝必须出声。** 管线遇到日期倒置、缺开始日、生效日在未来的源记录只报告并拒绝导入，
  不猜日期，也不靠 `max(1, ...)` 把错误区间伪装成一月经历。静默丢数据的 ETL 没人能验收。
- **不落没有读者的列。** 库里每一列都要能指出谁在读它（检索谓词或界面上的某处）。切段时的
  工作值（去重键、回填入口、匹配别名）算完即弃，不进库：存下来之后，下一个人无从判断它是
  废弃的还是漏用的。这条同样适用于 `org_meta` 里的 key。
- **不为「将来可能要」预留任何东西**：不留没人设置的类型字段、没人调用的导出、没有第二个
  使用者的 `export type`、没跑过的脚本。新能力在需求和读者真实出现时再加。

## 检索

- **限制只写在收窄的地方，不写在准入的地方。** 同一件事不许有两份契约：词长归 `parseQuery`
  （2~24 字），`intentSchema` 只在 `describe` 里写建议——把数字写成 schema 约束，模型多给
  一个长词就是**整条响应作废**、整句话退回规则解析，而收窄本来只会丢掉那一个词。
- **值域不许写成针对性枚举。** `minMonths` 是连续量（SQL 是 `months >= ?`），URL 校验、
  服务端收窄、查询理解三处同一条口径：正整数。筛选里那四个档是**分面的桶**
  （`MIN_MONTHS_BUCKETS`），是展示，不是值域。凡是「界面上只有这几个选项」就去锁死数据层
  值域的，都是把展示当成了契约。
- **打分不许写进 SQL。** `search.ts` 只产出事实（哪段以多高的相关度命中了哪个词、多少月、
  什么时候结束），打分、AND 判定、排序、分面全在 `rank.ts` 里对内存中的事实求值。这样调一次
  权重不必起数据库，排名与分面也不可能分家——它们对的是同一份事实。代价是候选要全部拉回
  内存，`FACT_MAX` 是那根保险丝。唯一的 SQL 内强度比较是**同一要求、同一经历段的去重**：
  一段四路、几个说法都可能过阈值，它必须在保险丝之前按 `ROUTE_WEIGHTS × 相关度` 只留一行，
  但不产生人员分数、不做准入或排序。换检索手段时，人员排名与分面仍只改 `rank.ts`。
- **匹配系数只许住在 `src/search/weights.ts`**，不要散进别处。各个常量为什么是那个值、
  两个地板的乘积为什么必须大于 0.5、`RELEVANCE_MIN` 的数从哪来，都写在那个文件里。
- **命中分两步、两个模型，各管一头，别让一个模型干两件事。** 向量只**召回**候选说法
  （`RECALL_MIN` 放得低，`RECALL_MAX` 是闸），重排模型才**判定**命中（`RELEVANCE_MIN`）。
  稠密向量在两三个字的短语上分不开「前端 / 后端」「团队管理 / 数据管理」，这是模型的
  性质不是阈值的问题；把召回地板抬高去冒充判定，会在丢掉真命中的同时留下这些反义。
  两步都在 `phrases.ts`，`search.ts` 拿到的已经是「命中的说法」。
- **成员资格只有一条门槛，而且不为任何一个词开特例。** 一段经历算不算命中一条要求，由
  相关度过没过 `RELEVANCE_MIN` 决定；AND 判定、总人数、分面「点了还剩几人」、量宽
  （`probeWide`）全建立在这条线上，所以它是全站常量，不许逐查询、逐路、逐筛选变。
  某个词搜错了（「产品经理」捞到「销售经理」），先问它暴露的是哪条机制的问题；机制修不了的
  残余错误由证据行暴露给用户改词——产品代码里不许出现某个具体的词。
- **语料侧和查询侧必须是同一个嵌入空间。** `EMBED_SPACE_ID` 标识模型、权重、归一化与端点
  实现的组合；任一项变化都换 id 并重跑 ETL。ETL 把 id、模型、维数和 fresh canary 写进
  `embedding_space`，查询进程首次嵌入前逐项核对并重新嵌 canary；不一致直接拒绝检索，
  不允许用空名单冒充答案。`EMBED_DIM` 仍须与 schema 同步。重排缓存按
  `RERANK_SPACE_ID` 键入；重排行为变化就换 id，语料重灌仍会随 phrase 级联清空旧分。
- **四路原文的拼法只有一个主人：`etl/embed.py` 的 `route_texts`。** 序列三级用「 · 」连、
  部门在公司内用 `org_path`、空文本不存向量——这些决定了库里的向量是从什么字符串来的。
  `tests/fixture.ts` 的 `routeTexts` 是它的**抄本**，不是第二个实现；改那边必须改这边，
  否则夹具嵌的和 ETL 嵌的不是一种字符串，测试全绿而线上不同。
- **专有名词永远不进向量。** 公司名、学校名在向量空间里和同类名字是邻居，语义匹配会把竞品
  一起捞进来；它们走 `org` / `school` 两个精确条件（`ILIKE`，输入必须过 `escapeLike`）。
  这条的执行点在查询理解的提示词里（路由规则）和 `intentSchema` 里（单独的字段），
  不在检索代码里——检索代码拿到什么词就嵌什么词。
- **`parseChips` 是全站唯一的查询解析入口**，服务端和页面都用它，`parseChips(toQuery(c)) === c`
  这条往返必须成立——否则改一枚 chip 会把别的也改掉。
- **停用（`~`）和强度正交，不是第四档强度。** 做成第四档的话，停一次再开就悄悄退化成
  「必须」，而这个改动在界面上没有任何提示。`~` 叠在强度符号**前面**，顺序定死，否则同一枚
  chip 有两种写法，往返就不再是恒等式。
- **排除词否决证据段，不否决人。** 检索对象是经历，排除也一样：命中排除词的段丧失为
  任何要求作证的资格，人只在失去全部证据时才出局。做成人级黑名单会把「实习起步、
  后来真干了八年」的人整个剔掉——那是给「从经历找人」这条第一原则开的反例。
- **进门的词可以模糊，赶人的词必须准。** 正向要求按 `RELEVANCE_MIN` 进门；排除词用更高的
  `RELEVANCE_MIN_EXCLUDE`，而且不带筛选。理由是「错了能不能被看见」：捞错了，人在名单上，
  看一眼就能纠正；否决错了，证据无声消失，永远没人知道。
- **说法之间 OR，要求之间 AND。** 一条要求的几个说法（用户并列的「A 或 B」）各自单独判定，
  满足其一即满足要求；同一段命中同一要求的两个说法只算一次。不要把模型译的
  同义词做成独立 chip：做成 must 是替用户加条件，做成 boost 则救不了「原词搜不到」的 AND。
  同义词这件事现在归嵌入模型管，模型只交词、不交数。

## 大模型

- **判断进提示词，度量衡进代码，事实问语料。** 一个片段是要求、是筛选还是不支持，哪种
  语气是哪档强度，是逐查询的判断，归提示词管、可以大改（记录不可变，改坏了不污染旧查询）；
  相似多少算命中、各路值多少分、结构长什么样，是全站的度量衡，只住在代码里（跨查询可比、
  有调参把手、用户改得动）；一个词在库里对应多少人，问语料（`probeWide` 量宽，落库前）。
- **模型适配层只有三个文件，都薄到没有逻辑**：`src/server/llm.ts`（查询理解）、
  `src/server/embed.ts`（嵌入）、`src/server/rerank.ts`（重排）。形状与收窄在
  `src/search/intent.ts`（纯函数），召回与判定的编排在 `src/search/phrases.ts`。想加第四处
  模型调用之前先问：这件事能不能做成「模型给建议、纯函数收窄、结果落在用户改得动的表示上」
  ——不能就别加。
- **三个端点的数据边界不一样，所以部署要求不一样。** 查询理解发出去的只有用户敲的那句话和
  语料里几维筛选的取值（公司档、职级、招聘渠道、学历），走公网 provider 没有顾虑；嵌入端点从 ETL
  那一侧收到的是**每个人的经历原文**，重排端点在查询时收到召回的岗位名、部门路径、简历描述，
  放公网等于把经历交给第三方——这是部署方按数据政策做的决定，代码不替它做，但文档必须把
  这个差别说清楚。姓名、工号永远不出这台机器；
  这也是「查询理解做、结果重排不做」的分界线——重排必须把候选人的经历发出去。
- **端点的失败方式不一样，别互相抄。** 查询理解没了可以退回规则解析，所以
  `understand()` 永远不抛、只返回 null，而且**降级要说出来**（规则解析读不出语气，
  「不要实习」会变成必须词，结果是错的而 chips 看起来完全正常；只在服务端 `console.warn`
  不算）。嵌入或重排没了检索什么都做不了，所以 `embed()` / `rerank()` 没配就抛、不降级——
  装作能用只会返回一份空名单或一份反义名单，而空名单在这个界面里的意思是「没有这样的人」。
- **模型输出是不可信输入**，必须过 `toDelta`，和 URL 过 `validateView`、服务端函数入参过
  `sanitizeFilters` 是同一档事。上游声明过 schema 不能省掉这一道。
- **模型给的词要过一遍 `parseQuery`**，保证它和用户敲的词走同一条路。少了这一道会得到一枚
  点一下就变形的 chip。
- **模型挑筛选值只能从语料词表里挑**（`vocabulary()`）。职级尤其如此：P5 到 P9 是这家组织
  的阶梯，另一家可能是 M1 到 M5，「总监」对应哪一档只有词表能回答，回答不了就该进
  `unsupported`——而不是塞进 terms 去和岗位名比相似度。
- 密钥走 `.env.local`，和 `DATABASE_URL` 同一个待遇。查询理解端点只用 POST，输入与模型输出
  都要有硬上限；公开部署时限流必须做在网关层，进程内计数器盖不住多实例。

## 服务端边界

- **服务端模块自己声明身份**：`#/db`、`#/search/search`、`#/search/phrases`、`#/server/llm`、
  `#/server/embed`、`#/server/rerank`、`#/server/turn` 顶上都有一行 `import "@tanstack/react-start/server-only"`。页面从它们取**值**会让构建失败，
  并打印完整 import 链。新写一个碰数据库连接或密钥的模块，就给它加上那一行。
  `#/db/schema` 不标（`drizzle-kit` 以 CJS 加载它，标了 `db:push` 起不来）。
- **服务端函数是可以被直接调用的端点。** 页面那侧的 `validateView` 管 URL，`search/params.ts` 的
  `sanitizeFilters` 管进程边界，两道都要有：少了后者，`minMonths: "abc"` 会进入数值比较并
  安静地筛掉所有人。
- **页面取服务端的值只有 `src/server/functions.ts` 一个口子。** `createServerFn` 切走的只是
  handler 的**函数体**，所以这个文件里服务端模块的值只许出现在 `.handler()` 里面。

## 界面

组件库是 coss ui，源码进仓库（`src/components/ui/`）。**升级或补组件之前先读
`src/components/ui/NOTICE.md`**——从哪个目录抄、为什么不能抄错目录，写在那里。

界面代码分三层，一个组件住哪儿由**谁在用它**决定，不由它长什么样决定：

| 目录 | 装什么 | 判据 |
| --- | --- | --- |
| `src/components/ui/` | 抄来的通用件（按钮、面、弹层） | 换个产品照样能用 |
| `src/components/` | 这个产品自己的展示件与视觉词汇（`query-bar` / `evidence` / `timeline` / `career-bar` / `brand` / `chip`） | 跨屏共用，但离开人才搜索就没有意义 |
| `src/routes/-components/` | 某一屏私有的（查询台、名单、零态） | 只有一个路由在用——两屏都要就往上走一层 |

`-` 前缀在 TanStack 里的语义就是「这个路由私有」。跨屏共用的东西留在那里，
导入路径会爬成 `../../-components/...`，而目录名还在说它是私有的——**名字说的和
事实不一样**，下一个人据此判断「改它会影响谁」就会判错。

- **凡是「一块面」或「一个控件」，都由 `src/components/ui/` 给。** 卡片、提示条、分隔线、
  按钮、开关、可点的标签——路由组件只挑组件、排布局、喂数据，不自己拼
  `rounded-* + border + shadow-*` 去画一块面。（吸顶的头给自己补一层 `bg-card` 挡住穿过去
  的内容不算画面：那是同一块面的延续，不是新的一块。）
  理由不是洁癖：coss 的身份不在颜色令牌里，在每块面上那层 `before:shadow` 顶光边、带色相的
  `shadow-xs/5`、`pointer-coarse` 触控目标和 `data-pressed` 按压态上。手写的面把颜色抄对了
  也一样都不会有，于是屏幕读起来是通用 Tailwind 而不是 coss——**换组件库最容易漏掉的
  就是这一层**，因为它不在令牌里，只在组件源码里。缺什么就从注册表补抄一个进来。
- **不自造色板、不写魔法像素值。** 颜色一律用语义令牌（`bg-card` / `text-muted-foreground`
  / `border-border` …），尺度用 `styles.css` 里已经定下来的那几档。要加一档，先回答现有的
  哪一档不够用。
- **色相各有一个主人，全站唯一**：绿（`success`）= 受控字段命中，蓝（`info`）= 选中，
  amber（`warning`）= 查询上的提示（未识别语气、不支持的条件）。品牌标记不占色相——
  coss 是中性色系统，`--primary` 是「最重的前景色」，不是一个品牌色。要给任何东西发第四个
  色相，先回答它为什么不会被读成上面三件事之一。
- **强度（必须 / 加分 / 排除）写在符号上，不写在颜色上。** 「必须」不带符号——它是默认，
  而默认不该有标记。
- **证据强度点阵是唯一需要学习的编码**，证据行、详情面板、时间轴、图例必须共用
  `#/components/evidence.tsx` 的 `Dot` 与 `BAND_FILL`，不要各画各的。三档靠**填充方式**
  区分而不是三种颜色，所以它在黑白和色弱下仍然成立——加一档或改成纯色相之前先想清楚这一条。
- **同一份数据只画一遍。** 这条最容易在评审里点头、在代码里违反。判断方法：把其中一个表示
  删掉，如果没有信息丢失，那它本来就是重复的。查询台上那排 chip 不属于复述——它是**唯一
  能改查询的地方**（输入框只负责添加条件），因此保留。
  详情面板尤其容易犯：人是从名单上那张卡片点进来的，而那张卡片已经逐词给过命中摘要，
  **且给得更细**（它有命中的字段值）。在详情里再画一份更少的摘要，这一栏就白开了——
  它的价值是逐段核对（轨迹条给形状、时间轴给原文），不是把卡片重说一遍。
- **一个决定只留一个执行点，而且必须是看得见的那一个。** 「一个人都没有时不报数」写在
  调用点——空态那一支不渲染表头——而不是写成 `ResultHeader` 里一条让它恒为 `null` 的守卫。
  藏进守卫的话，调用点看起来画了一个表头而屏幕上什么都没有，读代码的人得先在脑子里
  跑一遍守卫才知道结果。**判据**：把守卫删掉，如果任何真实输入下的画面都不变，那它就是死的。
- **靠注释维持的一致性等于没有一致性。** 「这里要和那边同高 / 同尺码」写成注释，改一处
  忘一处就是静默漂移，而症状（顶部跳一下、零态的按钮变了个样）没人会当成 bug 报上来。
  同一个值只许有一个出处：详情面板骨架和真身共用 `HEAD`，名单骨架和卡片共用 `PAD`，
  零态那排「常用方向」直接读 chip 自己的 `CHIP_SIZE` 与 `MODE_VARIANT.must`。
- **注释给的理由必须是真的，而且只解释在场的东西。** 「为了稳定身份」只能写在真的进了
  某个 effect 依赖数组的值上（`NO_RESULTS` 是，分面不是）；理由套错地方，下一个人会以为
  凡是空值都得提成常量。同样不要解释一个不存在的做法——「这里没有用 X」是变更说明，
  不是代码注释，它会让人以为 X 曾经在这儿。
- **不要做「覆盖度 n/m」这类指示器。** 必须词是 AND 语义，它恒等于满值；要表达的是每个词
  命中得有多硬。
- **版心只有一个数：`--container-page`（styles.css）。** 品牌行、查询台、名单、页脚各自
  `mx-auto w-full max-w-page px-4`，四处必须分毫不差——它们上下叠在一起，差 1px 就是一条
  看得见的错位。想改宽度就改那一个令牌，不要在任何一处写 `max-w-[46rem]`。注意它必须定义在
  `--container-*` 命名空间下：写成 `--spacing-page` 的话 `max-w-page` 会**静默**变成一个
  不存在的类，构建不报错。
- **画布必须比卡片暗一档**（`--color-canvas` vs `--card`）。coss 这两个令牌在浅色下都是纯白，
  差 0，台阶是这个项目自己指过来的。抹平它，一列候选人就退化成「铺满窗口、靠发丝线切开的
  平面」，也就是后台的长相。
- **字阶只有五档**，都在 `styles.css` 里命名：`display-1`（零态主标题）/ `title-1`（详情面板的
  姓名）/ `title-2`（卡片上的姓名）/ 正文 14px / `label`（分区标签）。要点在于**相邻两档拉得
  够开**：全挤在 12/14/16 里只差 2px，而汉字在小字号上没有拉丁字母那种 x-height 差异，扫过去
  就是一片没有重量差的灰。加第六档之前先回答它和哪一档的差别是什么。
- **分区标签走 `label`，不要用「12px + 次要色」冒充。** 正文里的上下文、时长、来源标签也是
  12px 次要色，两者长得一模一样时，分区的边界就只剩一条发丝线在扛。`label` 给的是 11px、
  600 字重、放开的字距——那是正文永远不会有的三样东西。
- **成段阅读的文本用 `read-cjk` 收到 34rem。** 那是行长，不是布局：容器窄于它时这条不起作用。
- **路由文件只接线，不产状态。** `createFileRoute` 那一层负责 loader、把数据喂给组件、
  排版；派生状态归 `-lib/` 的 hook（`useNavPhase` / `useInterpretation` / `useCommit` /
  `useKeyboardFlow`）。判据是**能不能测**：这里错一个布尔量不会有任何东西报错，
  只会在扫名单时每按一下就清空一次列表。所以 `navPhase` 是纯函数、`useNavPhase` 只负责
  从路由取两头，两种 pending 的每一种组合都钉在 `tests/nav-phase.test.ts` 里。
- **层级只有 `styles.css` 里那三档命名 z 值**（`z-raise` / `z-stick` / `z-escape`），代码里
  不许出现 `z-10` 这种裸数字，也不要靠给弹层补更大的数字——外壳根节点的 `isolate` 已经从
  结构上解决了这件事，理由写在那里。
- **窄屏（xl 以下）的详情用 `Dialog`，不要自己搭浮层。** 宽窄两套容器**只能二选一地渲染**
  （`media.ts` 的 `useIsWide`）：给 `Dialog` 加 `xl:hidden` 只藏得住样子，藏不掉焦点陷阱和
  滚动锁定，桌面上会留下一个看不见却抓着焦点的对话框。宽屏那一块必须保持**非模态**——它是
  把名单推开的兄弟节点，不是盖在上面的浮层，否则 ↑↓ 扫名单就废了。可及名称一律用
  `DialogTitle`（`sr-only` 即可）。
- **查询台是一块面，不是几条并排漂着的横带。** 输入框、chips、筛选装在 `Frame` 的浅底托盘
  加一块 `FramePanel` 里（`query-deck.tsx`）。三样东西左边缘都对齐在版心上、间距又都差不多的
  时候，眼睛读不出它们是一组还是各管各的；而这一屏里所有能操作的东西都在这块面上。托盘那一档
  比画布深、比卡片浅，操作面和结果面因此各是各的，不必再画一条分隔线。
- **输入框和它的提交按钮是同一块面**（`InputGroup` + `InputGroupAddon`），不是并排的两块。
  并排会得到两条顶光边、两个圆角、两套焦点环，读起来是两件事。按钮也不给实心主色：这一屏的
  主行动是「找人」，不是「往查询里加一个词」——给它最重的一档，屏幕上对比度最高的东西就成了
  那个次要动作。
- **chips 和范围筛选必须在形状上分得开。** chips 是散着的 `Button size="xs"`（一枚一个词），
  各个筛选维度连成**一条分段控件**（`Group` 裹住 `Select` 与 `Toggle`，`filter-bar.tsx`）。
  两排都做成同尺同色的小按钮时，唯一的分组线索是换行——而换行是布局的意外，窗口窄一点最后
  一枚 chip 就掉进筛选那一排里。分段控件的两个圆头就是这一组的边界，靠形状说话，不靠注释。
- **选一个值用 `Select`，切一个布尔量用 `Toggle`。** 别拿 `Menu` + `MenuRadioGroup` 冒充下拉
  选值（菜单是发命令的地方），也别在 `Button` 上手写 `aria-pressed` 再自配一套底色。
- **报数和图例属于名单，不属于查询台。** 「N 人 · 按相关度排序」和点阵图例合成名单的表头
  （`result-list.tsx` 的 `ResultHeader`）。报数挂在查询台右上角时，chips 一换行它就悬在两排
  之上，而它说的本来也不是「我搜了什么」。图例则必须和它解释的那些点同屏——别把它挪进任何
  「空着才显示」的位置（详情面板的空态是最诱人的那一个）：那类位置空着的时候，正意味着屏幕上
  一颗点都没有，于是**只在没有点的时候才解释那些点**——而这个错误全绿。
- **提示分两档：页面级的事件用 `Alert`，查询上的注解用一行小字。** 「没能提交」是前者，
  「这句话的语气没识别出来」是后者。给一句注解发一整块满宽 amber，它就成了整屏第二重的东西。
- **按压反馈和触控目标不要自己写。** coss 的每个控件都自带 `data-pressed` / `:active` 态和
  `pointer-coarse:after:min-h-11`——只在粗指针设备上撑开，桌面上不会造出一圈看不见的命中区。
  想用 `after:-inset-*` 手撑命中区之前先看一眼：伪元素带上 `pointer-events-none` 就一个点击
  都接不到，而这件事**没有任何检查会报**，只有拿手指去点才发现。
- **动效全站只有一处：换人时详情面板 `settle` 淡入。** 加第二处之前先删掉一处。
  同一件事也不要画第二遍——选中态已经由边框、环、`aria-current` 和推开的详情面板说了四遍，
  再加一根需要 `offsetTop` + `ResizeObserver` 量出来的滑轨，只是第五遍。

## 查询、视图与计数

- **查询是一条记录，视图是几个 URL 参数——这两样东西不许混。** 「我要找什么人」完整落成
  `SearchSpec`（证据要求、结构化范围、未生效提示）并保存在 `search_turn`，地址上只出现它的 id；「我怎么看这批人」
  （各个筛选维度、翻页数）留在 query string，当前员工由 `/p/:empId` 子路由表达。判据是**值不值得留存**：前者要能重新
  理解、能出现在最近搜索里、分享出去仍是同一个问题（复现的是条件，不是名单——
  名单跟着语料和时间走），后者一次性。
  - 写查询只有 `-lib/commit.ts` 的 `commit` 一个入口（不可重入 + 失败可见），写视图只有各
    路由的 `updateView` 一个入口。把它们并成一个函数就是这条边界消失的方式：改一个筛选和换
    一个问题在代码里长得一模一样，而它们的代价差着几个数量级。
  - 条件从哪来由 `spec.ts` 的 `QueryInput` 判别联合说话：`sentence` 过查询理解，`spec`
    直接落地。界面编辑必须提交完整 spec，不能只传 chips 再靠 URL 或父记录补齐其余含义。
  - **记录不可变。** 改条件一律派生新记录并 push 导航，于是后退键天然是撤销。`search_turn`
    只 INSERT，唯一的 UPDATE 是把 `delta` 与 `spec` 从 null 补成理解结果，且必须带
    `where spec is null`。`delta` 是这句话自己的产物，重译直接替换它，不从父 spec 反推。
  - **不要把查询串写回 URL。** 那样做原话就丢了（于是永远无法重新理解），理解结果被冻结成
    事实（模型改好了惠及不到任何一条已有链接），而「模型判成必须、人改成加分」这类修正——这个
    产品唯一能自己产出的模型评估数据——每次导航扔一次。
- **换人用 `replace: true`**（↑↓ 与点击）。换人是「在看哪一个」，不是一次导航；不 replace 的话
  扫过三十个人就往历史栈里压三十条，后退键和手机侧滑都失效。只有换查询、改筛选才 push。
- **理解不许挡在导航前面。** 整句提交只落一条记录就导航，模型那一跳由工作台自己补
  （`spec === null` 时触发）。模型单次可以从一秒跳到几十秒，让它挡在零态和工作台之间，人看到的
  是首页卡住了；转圈要发生在结果将要出现的地方，不是发生在按下去的那个按钮上。
- **翻页靠把 `limit` 调大重查，不用 offset。** 排序键是算出来的分数，第二页的语义得是「同一次
  排序里的第 51 到 100 名」，而 offset 只保证「再跑一次排序，跳过前 50」——两次跑之间只要有一条
  经历落库，就会有人重复出现或整个消失，界面上看不出来。改任何筛选都回到第一页。
- **翻页的 pending 和换查询的 pending 不是一回事。** 换查询要塌成骨架屏（旧结果已经不成立），
  翻页必须把已经看到的人留在原地，只让按钮转圈。而**换人不算换查询**——路径也变了，只比
  pathname 会让扫名单的每一下都把列表清空。
- **计数只有一个单位：人。** 筛选弹层里每一项后面的数是「点了它还剩多少人」，查询台右上角那个数是
  截断之前的总人数，页脚那句「已显示 X 人，共 Y 人」里的 X 才是这一页有多长。数段数或数页大小
  都会让同一屏出现两个互相拆台的数。**新增任何数字前先回答它的单位是什么。** 唯一的例外是三处
  以「项」为单位的筛选项计数，它们数的是控件不是人，且都紧挨着「项」字。
- **列表不许静默截断。** 只要 `total` 超过一页，页脚必须同时说出「已显示多少」「一共多少」
  「按什么排的」。翻不动时不留一个按不动的按钮，改说原因：「已经是全部了」和「到上限了」是两件
  事，后者要给出路。
- **显示出来的数，必须就是参与排序的那个数。** 显示某一段的月数而排序用累计月数，会让两个名次
  不同的人长得一模一样——一个把「可解释」写进产品原则的工具，最不能容忍的就是这个。
- **分数不上屏，名次上界面。** 分数没有刻度让人校准「1.35 算高还是低」；名次有（分母就在旁边）。
  名单上每张卡片前面那个序号和详情面板里那句「第 N 位，共 M 人」是同一个名次，别让它们分家。
- **对不上的数一个都不给。** 零态的方向不带人数：「算法 128」里的 128 是「有过算法序列经历的
  人数」，而点下去是一次检索，它还会从岗位、部门、简历原文命中另一批人，两个数必然对不上。
- **候选事实过多是一种结果，不是一次失败。** 事实超过 `FACT_MAX` 时走
  `-lib/empty-state.ts` 那套引导，不截断、不抛异常。语义查询按 `(要求, 经历段)` 的真实贡献量
  点名需要具体化的要求；纯结构化查询直接要求收窄范围。不能拿 `WIDE_SHARE` 的人数覆盖率代替：
  一把尺管内存载荷，一把尺管条件区分度，单位不同。
- **空态必须说准成因。** 六种零结果长得一模一样（都是零行），说错了没有任何断言会红：「没解析
  出词 / 条件全被停用 / 只写了排除词 / 证据要求滤空 / 筛选太窄 / 概念词太窄」，成因不同出路也
  不同，尤其「你自己把条件全停用了」和「真的没有这样的人」完全相反。
- **零态给出的每个入口都必须落得到人，而且落在它写着的那个词上。** 词汇表来自语料，不是手写的
  ——手写的例子会某天悄悄搜不到人。每个候选还要过一次 `parseQuery` 体检：「安全与风险合规」里的
  「与」是分隔符，点下去会变成两个词。一个点下去变形或落空的入口比不给这个入口更糟。
- **没处放的条件必须在界面上说出来**（`unsupported`）。用户写了「北京的」，屏幕上的条件里
  没有它，不说一句他会以为它生效了。它落库、随记录走、长在查询台 chips 的脚注位置，不在
  结果头上单独占一行：那是关于**这一句话**的事实，而那里正好是能立刻改它的地方。

## 全绿的陷阱

下面几条违反之后 `tsc` / `biome` / `build` 全过，只有人眼或用户能发现。

- **设计系统组件不是 HTML 标签。** 它们常把一件事拆成几份互不重叠的契约，只喂一份照样能渲染出
  一个「看着完整」的元素——比如只给 `children` 不给 `items`，选中后会把 `internal`、`12` 这种
  URL 原始值直接显示出来。写之前先看 coss 的组件文档或 `src/components/ui/` 里那份源码。
- **界面回归测试断言的是可见文本，不是属性**（`tests/render.ts` 的 `visibleText`）。它先把整个
  标签连同属性抹掉，只留文本节点。上面那个空壳带着 `aria-label`，按字符串搜 HTML 会命中，全绿，
  只有人眼能看出它是空的。凡是「这几个字必须让人看见」的地方，就用它钉住。
- **`notFoundComponent` 必须挂在抛 `notFound()` 的那个路由上**，不能挂到 `s/$turnId`。挂到外壳
  那一层会让**整个页面外壳被那一句话替换掉**，连同旁边那份还成立的名单。要指定由某个
  父路由接住，用 `notFound({ routeId })`。
- **详情面板画哪一份内容由路由决定，不由外壳里的三元表达式决定。** 各种状态各自属于一个路由，
  外壳只无条件渲染 `<Outlet />`。
- **筛选的 value→label 对照表只有一份**（`-lib/filters.ts`）。URL 里是 `kind=internal`，界面上是
  「公司内经历」，这中间没有任何类型约束，`tests/filters.test.ts` 是唯一的拦网。`filterFields` 在
  路由里算**一次**再往下传，两处各算一遍就有了两份可能不一致的同名东西。
- **分面必须跟着这一次检索走**，和名次出自同一份事实，所以没有独立的 `fetchFacets`。算某一维时
  要摘掉这一维自己的筛选，否则选中一项之后其余项全是 0，人就再也切不动了。
- **「证据要求」和其余各维一样是服务端筛选。** 放到客户端它就只能筛已经翻出来的那几页，而按钮上
  其余各维数的是全部命中的人——同一列控件两种口径。

## 分层

`-` 前缀被路由生成器忽略，是 TanStack Router 的官方约定。**按「谁在用」分，不按「像什么」分**，
而且按今天的使用者算，不按将来可能：

- `src/routes/-components/` 某一屏私有的组件。**两屏都要的东西一律往上走一层**——
  route-private 之间互相 import，等于目录名在撒谎；
- `src/routes/-lib/*.ts` 页面自己的非组件模块（URL 视图状态、值→标签、键盘流、宽窄分界、
  空态引导、提交查询）；
- `src/components/ui/` coss 的组件源码，抄来的，见那里的 `NOTICE.md`；其余 `src/components/`
  放自己写的跨屏展示件，以及它们共用的视觉词汇（`chip.ts` 的 chip 静息态、`evidence.tsx`
  的点阵编码）；与界面无关的纯函数进 `src/lib/`；
- `src/search/` 内部有一道分界：`result.ts` 是结果形状（页面可以取值），`search.ts` 带 `db`
  （页面只能取类型）。加字段时先问它属于哪一侧；
- `src/db/index.ts` 只出连接，表定义一律从 `#/db/schema` 取；
- 加筛选维度只改 `-lib/view-params.ts`、`-lib/filters.ts` 与 `search.ts` 的分面。

## 提交前

```bash
npm run verify   # check + typecheck + test:etl + test + build
```

`npm test` 需要一个可连的本地 Postgres（读 `.env.local` 的 `DATABASE_URL`）。检索语义有集成测试
（`tests/search.test.ts`）：它在临时 schema 上建一份合成夹具跑真 SQL，建表语句由 `schema.ts` 现场
推导，不手抄；嵌入和重排由进程内的假端点提供（字符袋向量，相关度可以手算），它钉的是机制，
不是语义质量——语义质量归 `npm run eval` 和真模型。改 AND 语义、打分公式、阈值或路径判定
之前先看它。
