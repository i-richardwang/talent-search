# talent-search 开发约定

运行方式、模块职责与数据来源见 [README.md](README.md)；产品定位见 `docs/PROJECT.md`，实施方案见 `docs/PLAN.md`。

- **真实员工数据不进版本库。** ETL 只从配置好的数据源读、只往 Postgres 写。不要把 parquet、
  导出的 CSV、查询结果或含真实姓名工号的样例写进仓库或文档。仓库里能出现的只有合成数据
  （`etl/sources/sample/`、`tests/fixture.ts`）。
- **数据源与管线之间只有一份契约**（`etl/contract.py`）。分界线是一句话：**换一套数据就得
  重写的，归适配器；换一套数据也必须成立的，归管线。**字段名、字典码、人群口径、切段规则、
  上游的已知缺陷全部留在 `etl/sources/<name>.py`；区间校验、封口、相邻段合并、时长与
  `cur_*` 派生只在 `etl/pipeline.py` 写一遍。管线里出现任何一个具体源的字段名，
  下一个接数据的人就会照着再写一套「什么算一段经历」。
  - **适配器默认是私有的。** `.gitignore` 用白名单放行 `csv_dir` 与样例，其余一律不进仓库——
    表名和字典码本身就是一家组织的内部信息。规则的方向很重要：新写一个适配器**天然**被忽略，
    「忘了加 ignore」这件事不可能发生。
  - **管线不许为某一个源开分支。** 源之间的差异用契约的取值表达（比如 `segment_key`），
    不用 `if source == ...`。
- **表结构的唯一事实源是 `src/db/schema.ts`**，改完跑 `npm run db:push`；Python 只 INSERT，不建表。
  - **drizzle 是 schema 与迁移工具，不是查询层。** 检索的 SQL 一律手写、集中在
    `search.ts`：命中判定要按路展开 CASE，打分口径要能逐条读，查询构建器两样都做不好。
    查询构建器只用在**按主键取实体**那一类（`functions.ts` 取单人时间线）。
    这条边界不写下来，下一个人顺手用它写一条检索，两种风格就并存了，而没有任何关卡会拦。
  改表只有 push 这一条路，不生成迁移文件——两套改表机制并存就是同一件事两个入口。
- **经历的不变量由数据库保证**：`kind` 只有 internal / external，`months > 0`，
  `end_date` 为空或不早于 `start_date`。管线遇到日期倒置、缺开始日、生效日在未来的源记录
  只报告并拒绝导入，不猜日期，也不靠 `max(1, ...)` 把错误区间伪装成一月经历。
  **拒绝必须出声**：静默丢数据的 ETL 没人能验收。
- **不落没有读者的列。** 库里每一列都要能指出谁在读它（检索谓词或界面上的某处）。
  切段时的工作值（去重键、回填入口、匹配别名）算完即弃，不进库：存下来之后，
  下一个人无从判断它是废弃的还是漏用的。这条同样适用于 `org_meta` 里的 key。
- **不为"将来可能要"预留任何东西**：不留没人设置的类型字段、没人调用的导出、
  没有第二个使用者的 `export type`、没跑过的脚本。新能力在需求和读者真实出现时再加。
- **限制只写在收窄的地方，不写在准入的地方。** 同一件事不许有两份契约：词长归
  `parseQuery`（2~24 字），`intentSchema` 只在 `describe` 里写建议——把数字写成 schema
  约束，模型多给一个长词就是**整条响应作废**、整句话退回规则解析，而收窄本来只会丢掉
  那一个词。
- **值域不许写成针对性枚举。** `minMonths` 是连续量（SQL 是 `months >= ?`），
  URL 校验、服务端收窄、查询理解三处同一条口径：正整数。左栏那四个档是
  **分面的桶**（`MIN_MONTHS_BUCKETS`），是展示，不是值域；不在桶里的当前值由
  `ensureSelected` 补回列表，照样可见可取消。凡是「界面上只有这几个选项」就去
  锁死数据层值域的，都是把展示当成了契约。
- **大模型只许出现在一个文件里**：`src/server/llm.ts`。它是薄适配层，没有逻辑；
  形状与收窄在 `src/search/intent.ts`（纯函数，`tests/intent.test.ts` 钉着）。
  想加一处模型调用之前先问：这件事能不能做成「模型给建议、纯函数收窄、结果落在
  用户改得动的表示上」——不能就别加。
  - **发给模型的只有两样东西**：用户自己敲的那句话，以及语料里的公司档取值。
    姓名、工号、任何一个人的经历都不许出这台机器。这也是「查询理解做、结果重排
    不做」的分界线：重排必须把候选人的经历发出去。
  - **模型输出是不可信输入**，必须过 `toIntent`，和 URL 过 `validateView`、
    服务端函数入参过 `sanitizeFilters` 是同一档事。上游声明过 schema 不能省掉这一道。
  - **改查询只有一个入口**（`-lib/commit.ts` 的 `commit`），条件从哪来由
    `parse.ts` 的 `QueryInput` 判别联合说话：`sentence` 过查询理解，`chips`
    直接落地。不要为这两者各留一个回调——它们的签名都是「给我一个 string」，
    接错了类型、渲染、构建全绿，只有语气（「最好」「不要」）被静默判成「必须」。
    零态那排序列按钮是 `chips`：它们是查询理解的产物，送回去让模型再猜一遍
    只会变坏，还要为一个从语料里取出来的词干等十几秒。
  - **提交查询只有一份实现**：`commit.ts` 的 `useCommit`（不可重入 + 失败可见）。
    写查询的入口不止一个（零态整句、示例、序列按钮、改 chip），而「落一条记录再
    导航」是它们共同的定义，不是各自的实现细节。有的入口落记录、有的直接改 URL，
    同一个产品就有了两种查询，而不落记录的那个查完就没了。
  - **模型不可用必须降级**成本地规则解析，不能让检索不可用。`understand()` 因此
    永远不抛，只返回 null。
  - **模型给的词要过一遍 `parseQuery`**：它保证模型给的词和用户敲的词走同一条路，
    `toQuery` 写进 URL 再 `parseChips` 读回来必然一致。少了这一道会得到一枚点一下
    就变形的 chip。
  - 密钥走 `.env.local`，和 `DATABASE_URL` 同一个待遇。
  - 查询理解端点只用 POST，输入与模型输出都要有硬上限。公开部署时还必须在网关层限流；
    进程内计数器无法覆盖多实例，不能冒充部署级限流。
- **打分不许写进 SQL。** `search.ts` 只产出事实（哪段命中了哪个词、多少月、什么时候结束），
  打分、AND 判定、排序、分面全在 `rank.ts` 里对内存中的事实求值。这样调一次权重不必起
  数据库，排名与分面也不可能分家——它们对的是同一份事实。代价是候选要全部拉回内存，
  `FACT_MAX` 是那根保险丝。换检索手段时改的是取数层，`rank.ts` 不动。
- **时长与近因永远压不过路权重。** 两个地板的乘积必须大于相邻路权重的最小比值
  （0.8 × 0.7 > 0.5），否则一个「干了很久」的部门命中会反超序列命中，`ROUTE_WEIGHTS`
  那套证据强度的论证就被算法推翻了。改地板前先读 `tests/rank.test.ts` 的第一个 describe。
- **匹配权重改 `src/search/weights.ts`**，不要把系数散进别处。
  `ROUTE_ORDER` 直接按权重推导路径判定顺序；同权重保留声明顺序。
- **可检索字段的清单只有一份**：`search.ts` 的 `ROUTE_FIELDS`。命中判定（`matchExpr`）、
  路径判定（`routeExpr`）和整词松弛（`relaxTerm`）都从它派生，加一个可搜字段只改这一处。
- **中文检索只能用 `ILIKE '%词%'`**：`to_tsvector` 不切中文，`pg_trgm` 相似度在中文上恒为 0 附近。
  当前不提供语义相似能力；`similarity()` 不能作为中文检索方案。
  - **用户输入必须过 `escapeLike`**：`%` `_` `\` 是 ILIKE 的元字符。不转义的话「%%」
    两个字符就让每一行恒真并扫描全表；「客户_经理」的下划线会变成通配符。
  - **`seq_l1/l2/l3` 必须有 `gin_trgm_ops` 索引。** 命中判定是 `ROUTE_FIELDS` 展开出的
    **七个字段**的 OR，只要有**一个字段**不可索引，整条 OR 就退化成顺扫——少了这三个，
    另外四个 trgm 索引一次都用不上（失效的单位是字段，不是路）。
    加可检索字段时同步加索引，并检查执行计划仍使用 BitmapOr。
  - **两字中文用不上任何 trgm 索引**（提不出 trigram），只能顺扫。这是下限，不是 bug——
    而两字恰好是最常见的概念词长度，所以这组索引的真实作用是「长词快，短词靠表小」。
    32136 段实测：`%渠道运营%` 走索引 0.63ms，`%风控%` 顺扫 5.9ms。语料涨一个数量级
    之后短词就是墙，那时换中文双字索引（pg_bigm）或分词器，不是加机器。**现在不预埋**
    （见 `src/db/schema.ts` 索引块上的注释）。
- **`relaxTerm` 只探语料，不带筛选。** 词退到哪一步只能由查询串和语料决定。让筛选参与的话，
  同一句话在不同筛选下会变成不同的词，于是**收窄筛选反而可能搜出更多人**，
  而左栏分面是按当前这个词算的，它承诺的「点了还剩几人」
  会全部落空。`tests/search.test.ts` 的「退到哪一步只由查询串和语料决定」钉住这条。
- **每个概念词带一个要求强度：必须 / 加分 / 排除**（`parse.ts` 的 `ChipMode`）。
  查询串里写成 `渠道运营,+带团队,-实习`，半角逗号是强度分组的边界，其余分隔符仍归
  `parseQuery` 切词。`parseChips` 是全站唯一的查询解析入口，服务端和页面都用它；
  `parseChips(toQuery(c)) === c` 这条往返必须成立，否则改一枚 chip 会把别的也改掉。
- **停用（`~`）和强度正交，不是第四档强度。** `~+带团队` 是「停用了的加分词」，
  重新启用回来还是加分词；做成第四档强度的话，停一次再开就悄悄退化成「必须」，
  而这个改动在界面上没有任何提示。写法上 `~` 叠在强度符号**前面**，顺序定死，
  否则同一枚 chip 有两种写法，往返就不再是恒等式。
  停用的 chip 在 `search()` 的第一行（`activeChips`）就消失了——检索、打分、
  分面、表格列一个都不必知道有「停用」这回事，这是它只花一个字段的原因。
  它存在的理由是**迭代**：想知道某个条件是不是太窄，代价该是两次点击，
  而不是删掉再手打一遍（删除会连强度一起丢掉，且不可撤销）。
- **AND 语义由 `rank.ts` 的 `complete` 执行，不是由分数碰巧归零。** 它只检查必须词；
  加分词不进门槛，只进分数。证据要求同样只管必须词——要求一个可有可无的词必须有
  受控证据是自相矛盾的。
- **加分只抬不压，没命中的加分词不产生任何因子。** 多个加分词会累积，也可能改变
  不同证据档位之间的顺序；这是“同时满足更多偏好”的明确产品语义，不受“时长与近因
  不跨档位”那条约束限制。
- **排除词既不松弛，也不带筛选。** 松弛用在排除上是一把没瞄准的枪（「量子炼金」退成
  「金」会把干过「资金」「基金」的人整片剔掉，而界面上只显示用户写的那个词）；
  带筛选则会让「只看在职经历」放出本来被排掉的人。`search.ts` 先取完整排除名单，
  再从事实中统一剔除，排名和五个分面共同消费剩下的同一份事实。
- **服务端模块自己声明身份**：`#/db`、`#/search/search`、`#/server/llm`、`#/server/turn`
  顶上都有一行 `import "@tanstack/react-start/server-only"`。页面从它们取**值**会让构建
  失败，并打印从入口到违规那一行的完整 import 链。新写一个碰数据库连接或密钥的模块，
  就给它加上那一行。
  `#/db/schema` 不标：它只依赖同构的 `drizzle-orm/pg-core`，而且 `drizzle-kit` 以 CJS
  加载它，标记只导出 `import` 条件，加上去 `db:push` 起不来。
  `import type` 一律不算，编译期就擦掉了——所以检索结果的**形状**住在 `#/search/result`
  （一行 SQL 都没有），`#/search/search` 只出 `search()` 和 `sanitizeFilters`。
- **服务端函数是可以被直接调用的端点。** 页面那侧的 `validateView` 管 URL，
  `search.ts` 的 `sanitizeFilters` 管进程边界，两道都要有：少了后者，
  `minMonths: "abc"` 会进入数值比较并安静地筛掉所有人。
- **UI 一律用 Kumo（`@cloudflare/kumo`，Cloudflare 出品，MIT）**，不要手搓组件、不要引第二套组件库。
  - **只用语义令牌**：`bg-kumo-base`、`text-kumo-subtle`、`border-kumo-hairline` 等，禁止 `bg-blue-500`
    这类原始色；深浅色由 `light-dark()` + `color-scheme` 自动决议，**不要写 `dark:`**。
  - **`Text` 不接受 `className`**（组件库刻意封掉了）。布局类挂到外层元素上，别用 `DANGEROUS_className`
    绕过；`mono` 系列的 `size` 只有 `"lg"`。
  - `src/styles.css` 的三段顺序不能动：Kumo 样式 → tailwindcss → `@source`。少了 `@source`，
    Tailwind 扫不到 `node_modules` 里 Kumo 的类名，弹层会掉样式。
  - 路由链接走 `LinkProvider`（`src/components/app-link.tsx` 把 `href` 桥到 TanStack Router），
    页面里直接写 `<Link href="...">`。
  - 查组件用本地 CLI：`npx @cloudflare/kumo ls` / `doc <Component>`，别照记忆写属性。
- **视觉方向是「密集档案感」**，取舍都按这个判：强排印层次、发丝线做主要分隔、
  不用阴影分层。它像一份严肃的内部人事档案，不像一个后台系统。
  **但密度只发生在数据行内部，界面骨架不参与。** 表格单元格 10px 的横向内边距是
  密的；顶栏、左栏、详情栏的 16px 竖线不是。把表格的密度涂满整个界面，得到的
  不是密集，是一张没有落点的灰网——那不是这个方向，那是这个方向没做完。
  界面上唯一需要学习的编码是**证据强度点阵**（实心橙 / 实心灰 / 空心），它是设计签名，
  表格、详情栏、时间轴、图例必须共用 `-components/evidence.tsx` 的 `Dot`，不要各画各的。
  **橙色的类名是 `bg-kumo-badge-orange`，不是 `bg-kumo-brand`。** Kumo 的
  `--color-kumo-brand` 是蓝色 `oklch(0.577 0.232 260)`；橙色只活在
  `--text-color-kumo-brand`（只能上文字）和 `--color-kumo-badge-orange` 上。
  写成 brand 点就是蓝的，和选中态同族，而同义的 `Badge variant="orange"`
  确实是橙——同一个含义两个色相，且 tsc / biome / build 全绿。改色前先去
  `node_modules/@cloudflare/kumo/dist/styles/theme-kumo.css` 确认令牌的真值。
  动效全站只有一处签名动作：换人时中栏那根选中轨平移 + 详情栏 `settle` 淡入。
  再加第二处之前先删掉一处——两层动画叠在一起节奏会互相干扰。
- **层级只有 `styles.css` 里那三档命名 z 值**（`z-raise` / `z-stick` / `z-escape`），
  代码里不许出现 `z-10` 这种裸数字。更要紧的是 `s/$turnId/route.tsx` 根节点那个 `isolate`：
  Kumo 的弹层 portal 到 `document.body` 且**自身不带 z-index**，而 CSS 里
  「z-index: auto 的定位元素」画在「z-index > 0 的」之前，所以外壳里随便一个正数 z
  都能盖住所有弹层。`isolate` 把外壳内部
  的层级全部关进一个层叠上下文，这类遮挡从结构上不可能发生。**不要靠给弹层补更大的数字。**
- **界面一律用 Kumo 的语义令牌和组件，不自造色板、不写魔法像素值。**
  - 水平轴统一 16px：顶栏、左栏、表格首末列、详情栏的左边缘必须对齐成一条线。
    左栏是 `px-1.5` 容器 + `px-2.5` 选项凑出来的，改任意一边都要让和仍然是 16。
  - 圆角三档：`rounded-control`（4px）给贴着文字的
    可交互物（键帽、原文标记、姓名链接、小图标按钮），`rounded-sheet`（6px）给真正的面
    （左栏选项行、时间轴上的一段经历），`rounded-panel`（10px）只给工作台内容面板。
    前两档与 Kumo 自身取值对齐；Kumo 组件自带的圆角不要改。
  - 字号只用 Kumo `Text` 的四档（`lg` 16 / `base` 14 / `sm` 13 / `xs` 12），不写 `text-[11px]` 这类值。
    分工是固定的：**16 只给顶栏那个产品名**（画布上的身份，不是内容），
    **14 是内容**（表格正文、姓名、时间轴的公司岗位），
    **13 是次要信息、分区标题，以及所有成段的密集文本**（简历原文、说明性正文），
    **12 只给标签**（表头、左栏组标题、职级、计数、图例说明、chip）。
    整屏都用同一档就没有层次可言，眼睛找不到落点。
    成段文本压到 13 有两个理由：它通常是一张卡片里最长的一块，跟卡片标题同为 14
    时卡片内部就没有层次了；而它在这个产品里恰好又是最弱的一路证据。
    12 不行——那是标签档，成段汉字用它连读会累。行高与行长由 `read-cjk` 管。
  - **同一行里角色相同的标注必须同一档。** 时间轴那条元数据行（起止年月 · 时长、
    序列、公司属性）说的是同一类东西，所以全是 13：`mono` 变体在 Kumo 里恒是 13
    （只有 `size="lg"` 能把它抬到 14），于是另外两个跟到 13，而不是把日期压到 12。
    跨行的主次一档就够（标题 14 / 下一行 13）；同一行内要拉开主次得两档
    （「匹配依据」里 14 的词 + 12 的说明），因为 1px 的差在并排时看不出来。
    两处例外：`styles.css` 的 `display-1`（28px，只给零态主标题），
    以及详情栏姓名的 `text-xl`（20px，Kumo 最大只到 16 且 `Text` 不收 `className`）。
    要加第三处，先回答「它凭什么也是这一层的锚点」。
  - **工作台的骨架是「画布 + 浮起来的内容面板」，不是铺满窗口的三栏。**
    顶栏和左栏坐在画布（`bg-kumo-canvas`，由 `__root.tsx` 的 `body` 给）上，两者都没有
    底色、没有边框；中栏和详情栏一起装进一块 `rounded-panel` + 发丝边框 + `bg-kumo-base`
    的面板，左右下留 12px 让画布透出来（上边由顶栏那 56px 承担，加了 `pt` 就会让
    左栏标题行和结果头错开一档）。中栏与详情栏之间只有一条发丝线——它们是同一件事的
    两半（看哪些人 / 看这一个人），不该是两块各自浮着的板子。
    这是「像产品」和「像后台系统」的分界，不是圆角有几个像素的问题：**内容有没有自己的
    边界，这件事必须被表达出来。** 深度仍然不用阴影，靠的是边框加画布。
  - **画布必须比面板暗一档，这条不能退。** Kumo 自己的浅色画布是 98.75%，面板是
    100%——差 1.25%，屏幕上看不出来，于是「浮起来」只是注释里的说法，实际长相是
    「铺满窗口、靠发丝线切开的平面」。`styles.css` 把浅色画布压到 `oklch(96.5%)`
    （深色本来就是 10% vs 17%，不动）。选择器写成 `:root:not([data-mode="dark"])`
    是为了压过 Kumo 在 base 层里那条同名声明，同时不碰深色。改画布或面板底色之前，
    先量一遍两者的明度差：**低于 3% 就等于没有面板。**
  - **界面的上沿是一条 56px 的水平线，三处必须对齐**：画布上的顶栏（`h-14`）、
    左栏的「筛选」标题行（`h-14`）、面板里那条结果头（`py-3` + `min-h-8`），
    以及详情栏那个吸顶的头（`h-14`）。两块并排的面板各画一条高度差十几像素的横线，
    是「这界面没人量过」最直接的证据。详情栏的头因此只放姓名和工号一行，
    「部门 · 岗位 · 职级」在正文里。
  - **纵向有三个带，不许混**：数据密度 8–10px（单元格、chip 内边距），
    结构留白 12px（面板与画布之间）与 24px（左栏组之间），控件行高 24 / 32 / 37px
    （chip / 左栏选项 / 表格数据行）。哪儿都一样密就是没有层次——密度该留在数据行
    内部，界面骨架不参与。
  - **面板里不许有死空白。** 中栏按 `mainBasis()`（姓名 + 岗位列上限 + 概念词列）
    收口，用不掉的宽度全部让给详情栏（`shrink-0 basis-[26rem] grow`），表格在中栏里
    是 `w-full`。反过来做——表格自己封顶、中栏吃满——得到的是一张贴着左边、右边空着
    一两百像素的表，连滚动条都画在那片空白的最外侧，而表头那条线和结果头那条线不一样长。
    宽度只许封在**栏**上，不许封在栏里的内容上；成段阅读的文本例外，它在 `read-cjk`
    里自己收到 34rem，那是行长不是布局。
    同理，顶栏的品牌块占住左栏那么宽，查询框的左边缘才落在面板的左边缘上。
  - **表格必须有横向结构。** Kumo 只在表头底下画一条线，行与行之间什么都没有；
    斑马纹在这张表上是关掉的（四五列用不上），所以行分隔线不能再省，
    否则五十行就是一片锁不住行的灰。线挂在 `td` 上而不是 `tr` 上
    （`border-collapse: separate` 下 `tr` 的边框不绘制），最后一行不画，
    那条线由页脚的上边框接管。
  - **表格里显示的数，必须就是参与排序的那个数。** 词分 = 强度 × 时长 × 近因
    （`rank.ts` 的 `termValue`），三个因子在证据格子里各有一处形态：点的填充、
    右对齐的年（取 `basis.months`，最强那一路上的**累计**月数）、以及数字的深浅
    （还在做的提到 default）。显示某一段的月数而排序用累计月数，会让两个名次
    不同的人在表格上长得一模一样——一个把「可解释」写进产品原则的工具，
    最不能容忍的就是这个。同理，「前」这个前缀由 `basis.external` 决定
    （累计的段是否全部来自入职前），不由某一段的 kind 决定。
  - **排序依据常驻在结果头上**，不是只在结果被截断时才在页脚出现一次。
    五十人以内的列表同样是排过序的，「从上往下看」这个动作要有依据。
  - 橙色全站只表达一件事：受控字段命中。不做装饰、不做选中态。
    唯一的例外是顶栏那枚品牌标记（`text-kumo-brand`）：编码活在面板**里面**，
    标记在画布上，两者永远不并排出现；而少了它第一屏没有任何东西说明这是哪个产品。
    要加第二处品牌色，先回答「它为什么不会被读成一次命中」。选中态是蓝
    （`kumo-info-tint`），提示是黄（`kumo-warning`，只给整词退子串那一条），
    检索高亮不占色相（`bg-kumo-fill` + 字色提到 default）。时间轴的节点画的是
    「这一段的证据有多硬」，不是「这一段命中了」——后者会让橙色在换个视图之后
    改变含义。
  - **窄屏（xl 以下）的左栏与详情栏是 Kumo 的 `Dialog`，不是自己搭的浮层。**
    盖住整个工作台的东西就该是真模态：焦点关在里面、背景不可 Tab、Esc 收起、
    关掉还焦——这些不要手写，`Dialog` 底下是 Base UI 的 `FloatingFocusManager`。
    宽窄两套容器**只能二选一地渲染**（`media.ts` 的 `useIsWide`）：给 `Dialog`
    加 `xl:hidden` 只藏得住样子，藏不掉焦点陷阱和滚动锁定，桌面上会留下一个
    看不见却抓着焦点的对话框。宽屏那一栏则必须保持**非模态**，否则 ↑↓ 扫表格就废了。
    `Dialog` 只收 `className` / `children` / `size` / `style` / `container`，
    传不进 `aria-label`——可及名称一律用 `Dialog.Title`（`sr-only` 即可）。
  - **换人用 `replace: true`**（↑↓ 与点行）。换人是"在看哪一个"，不是一次导航；
    不 replace 的话扫过三十个人就往历史栈里压三十条，后退键和手机侧滑都失效。
    只有换查询、改筛选才 push。
  - **计数只有一个单位：人。** 左栏每一项后面的数是「点了它还剩多少人」，
    中栏那句「共 N 人」是截断之前的总人数（`rank()` 的 `total`），
    表格页脚那句「已显示 X 人，共 Y 人」里的
    X 才是这一页有多长。数段数或数页大小都会让同一屏出现
    两个互相拆台的数。**新增任何数字前先回答它的单位是什么。**
  - **列表不许静默截断。** 只要 `total` 超过一页，表格页脚必须同时说出
    「已显示多少」「一共多少」「按什么排的」。少了任何一样，读者都无法分辨
    「这就是全部」和「后面还有五百个」，而前 N 个是不是最该看的 N 个也无从判断。
    翻不动的时候不留一个按不动的按钮，改说原因：「已经是全部了」和
    「到上限了（要看全得先收窄查询）」是两件事，后者要给出路。
  - **翻页靠把 `limit` 调大重查，不用 offset。** 排序键是算出来的分数，
    第二页的语义得是「同一次排序里的第 51 到 100 名」，而 offset 只保证
    「再跑一次排序，跳过前 50」——两次跑之间只要有一条经历落库，就会有人
    重复出现或整个消失，界面上看不出来。重拉一遍前 n 个总是自洽的。
    页数住在 URL 的 `n` 里（只认整页倍数，默认值不写进 URL），
    改任何筛选都回到第一页（收在 `route.tsx` 的 `updateView` 一个口子上）。
  - **查询是一条记录，视图是几个 URL 参数——这两样东西不许混。**
    「我要找什么人」（那句原话、理解出来的 chips）落成 `search_turn` 的一行，
    地址上只出现它的 id；「我怎么看这批人」（五个筛选维度、翻页数、看着谁）
    留在 query string 里。判据是**值不值得留存**：前者要能重新理解、能出现在
    最近搜索里、能分享出去必然复现，后者一次性。
    - 写查询只有 `-lib/commit.ts` 的 `commit` 一个入口，写视图只有各路由的
      `updateView` 一个入口。把它们并成一个函数就是这条边界消失的方式：
      改一个筛选和换一个问题在代码里长得一模一样，而它们的代价差着几个数量级。
    - **记录不可变。** 改条件一律派生新记录并 push 导航，于是后退键天然是撤销。
      `search_turn` 只 INSERT，唯一的 UPDATE 是把 `chips` 从 `null` 补成理解结果，
      且必须带 `where chips is null`。
    - **不要把查询串写回 URL。** 那样做原话就丢了（于是永远无法重新理解），
      理解结果被冻结成事实（模型改好了惠及不到任何一条已有链接），
      而「模型判成必须、人改成加分」这类修正——这个产品唯一能自己产出的
      模型评估数据——每次导航扔一次。
  - **理解不许挡在导航前面。** 整句提交只落一条记录（一次 INSERT）就导航，
    模型那一跳由工作台自己补（`interpretTurn`，`chips === null` 时触发）。
    模型单次可以从一秒跳到几十秒，让它挡在零态和工作台之间，人看到的是首页
    卡住了；转圈要发生在结果将要出现的地方，不是发生在按下去的那个按钮上。
  - **降级要说出来。** 模型不可用时退回规则解析，而规则解析读不出语气：
    「不要实习」会变成必须词，结果是错的而 chips 看起来完全正常。所以
    `resolveIntent` 要把 `degraded` 记在记录上，界面明说并给出「重新理解」。
    只在服务端打一行 `console.warn` 不算——用户永远发现不了。
  - **「查询太宽」是一种结果，不是一次失败。** 命中段超过 `FACT_MAX` 时
    `fetchFacts` 返回 `null`，`search()` 给出 `tooWide: true`，走 `-lib/empty-state.ts`
    那套引导。抛异常会把这个产品最容易触发的状态（搜「运营」这种两字宽词）
    渲染成一个报错页，而旁边就摆着一整套为空结果写好的文案。
  - **翻页的 pending 和换查询的 pending 不是一回事。** 换查询要塌成骨架屏
    （旧结果已经不成立），翻页必须把已经看到的人留在原地，只让按钮转圈——
    列表一塌，人就被扔回页首，滚动位置和刚才看到哪儿全丢。
    两者靠 `onlyMore(location, resolvedLocation)` 分开，而**换人不算换查询**：
    `/s/x/p/a` → `/s/x/p/b` 路径也变了，只比 pathname 会让扫表格的每一下都把表清空，
    所以先比 turnId（`turnOf`）。
  - **分数不上表，名次上详情栏。** 分数是路权重 × 时长因子再乘上加分项的积，
    没有刻度让人校准「1.35 算高还是低」，摆进表格只是一列读不出意思的数；
    而表格的列宽预算（`COL_WIDTH` 那段）本来就没有第三个固定列的位置。
    名次有刻度（分母就在旁边），放在详情栏「匹配依据」的开头，
    紧接着逐词说明分数由什么构成。
    唯一的例外是三处以「项」为单位的筛选项计数（「清除 N 项」「筛选 · N」「还有 N 项」），
    它们数的是控件不是人，且都紧挨着「项」字。要加第四个非人单位的数，先回答它凭什么。
  - 「证据要求」和其余四维一样是服务端筛选（`SearchFilters.strong`）。
    放到客户端它就只能筛已经翻出来的那几页，而左栏其余四维
    数的是全部命中的人——同一列控件两种口径。
  - 触控目标 40px 之外还有一条：**按压缩放只给紧凑控件**。整行宽的列表项要带
    `data-press="off"`（见 `styles.css`），否则缩 3% 会让文字横移三四个像素。
  - 中文不加负字距。`display-1` 里没有 `letter-spacing` 是故意的：收紧字距是
    拉丁排印的规矩，汉字字面本来就撑满字身，再收就是挤笔画。
  - **同一份数据只画一遍。** 这条最容易在评审里点头、在代码里违反。典型形态：
    表格格子里写「● 序列 2 年」（点的填充由 route 推导，文字又写一遍 route）、
    时间轴左边单列起始月（卡片第二行已有完整起止和时长）、
    详情栏用点阵 + Badge 颜色 + route 文字把同一个强度画三遍。
    判断方法：把其中一个表示删掉，如果没有信息丢失，那它本来就是重复的。
    中栏顶上那排概念词 chip 不属于复述：它是**唯一能改查询的地方**（表头改不了
    强度，输入框只负责添加条件），因此保留。
  - **可点的东西要有按压反馈，触控目标不小于 40px。** 两条都在 `styles.css` 的
    base 层统一定义（`scale: .97` + `touch-action`），不要每个按钮各写一次；
    视觉尺寸小于 40px 的控件用 `after:-inset-*` 往外撑命中区，不要改视觉尺寸。
    左栏那三类控件（选项、清除、还有 N 项）尤其不能省：它们是筛选的唯一入口，
    而 xl 以下它们只出现在那个触屏上的筛选对话框里。
  - 布局是三栏工作台：左栏筛选（`-components/filter-rail.tsx`，常驻索引不是弹层）、
    中栏结果（`-components/result-list.tsx`）、右栏详情。**三栏在 `xl`（1280）才展开，
    不是 `lg`**：1024 上左栏 224 + 详情栏 416 之后中栏只剩 384，而表格的 min-width
    是 600，那一整段中栏都在横滚。改列宽时重算这道加法。xl 以下左右两栏各自变成模态对话框。
    `s/$turnId/route.tsx` 拿检索结果，**不要把选中的人做成搜索参数**——
    那会让每次换人都重跑一次 SQL。
  - 筛选的 value→label 对照表只有一份：`-lib/filters.ts`。URL 里是 `kind=internal`，
    界面上是「公司内经历」，这中间没有任何类型约束，`tests/filters.test.ts` 是唯一的拦网。
    `filterFields` 在 `route.tsx` 里算**一次**再传给左栏和中栏，两处各算一遍就有了
    两份可能不一致的同名东西。`Facets` 的键与 `search.ts` 里 `Dim` 的五个维度名逐字一致
    （`seq` / `companyTag` / `kind` / `minMonths` / `strong`），加一维时不必再记一层映射。
    候选与人数由 `rank()` 一次算出，**没有独立的 fetchFacets**：分面必须跟着这一次
    检索走，而且和名次出自同一份事实。算某一维时要摘掉这一维自己的筛选（`keeps` 的
    `except`），否则选中一项之后其余项全是 0，人就再也切不动了。
    数不出人的选项不会出现——左栏能变短靠的是计数变准，不是折叠得更狠。
  - **每一栏都有宽度上限，因为这个应用的宽度上限约 1700px。** 左栏 14→15rem
    （最长的序列标签就到头了）、岗位列 12→22rem（最长约 30 个汉字）、
    概念词列恒 6rem、详情栏 26→34rem（中文舒适行长 30-40 字，再宽跟不住行）。
    超出之后富余留在**表格右侧**，两侧栏仍钉在屏幕边缘：位置记忆在任何宽度下
    都成立，而且屏幕边是零成本命中区。不要改成整个工作台居中。
    没有上限时岗位列会吃掉全部富余（2560 宽下它独占 1448px），姓名与证据点
    之间拉出一条空白河，而横向扫这张表正是主任务。
    **行的 hover 与选中底色必须止于表格右边缘**：铺满中栏的话，悬停会出现
    一条一千多像素的高亮长条，比空白难看得多。
  - 表格里的时长用 `lib/format.ts` 的 `years`（「2.3 年」），详情栏和时间轴用 `duration`
    （「2 年 3 个月」）。表格那一列 6rem 装不下精确写法，而靠 `truncate` 收场
    是把放不下这件事推给读者；折成一位小数的年之后宽度恒定，右对齐加
    `tabular-nums` 才是一列能上下比大小的数。
  - ↑↓ 换人时表格行靠 `scroll-mt-*` 避开吸顶表头，没有它选中行会滑到表头底下；
    焦点进了详情栏（`[data-pane=detail]`）就把方向键还给那一栏。
  - 必须词是 AND 语义，每个结果必然命中全部必须词。**不要做"覆盖度 n/m"这类指示器**，
    它在必须词上恒等于满值；要表达的是每个词命中得有多硬。加分词的有无本来就
    写在表头那个 `+` 和格子里有没有点上，不必另造一个计数。
  - **零态给出的每个入口都必须落得到人，而且落在它写着的那个词上。**
    词汇表来自语料（`overview()` 取二级序列），不是手写的——手写的例子会
    某天悄悄搜不到人。每个候选还要过一次 `parseQuery` 体检：「安全与风险合规」
    里的「与」是分隔符，点下去会变成两个词，和屏幕上写的不是同一个查询。
    一个点下去变形或落空的入口比不给这个入口更糟，两种坏法在页面上都看不出来。
    `tests/search.test.ts` 的「零态的语料概览」逐个点一遍。
  - **对不上的数一个都不给。** 零态的方向不带人数：「算法 128」里的 128 是
    「有过算法序列经历的人数」，而点下去是一次检索，它还会从岗位、部门、
    简历原文还会命中另一批人，两个数必然对不上。规模只说一次，说在顶上，
    因为没有任何东西能和它对不上。
  - **空态必须说准成因。** 六种零结果长得一模一样（都是零行），说错了没有任何
    断言会红：「没解析出词 / 条件全被停用 / 只写了排除词 / 证据要求滤空 /
    筛选太窄 / 概念词太窄」，成因不同出路也不同，尤其「你自己把条件全停用了」
    和「真的没有这样的人」完全相反。`emptyState` 因此是导出的纯函数，
    由 `tests/empty-state.test.ts` 逐条钉住配哪句话、按钮按下去去哪。
  - 强度（必须 / 加分 / 排除）写在符号上，不写在颜色上：色相已经各有其主
    （橙 = 受控命中、蓝 = 选中、黄 = 退子串）。「必须」不带符号——它是默认，
    而默认不该有标记。
  - 整词退到子串（`relaxTerm`）必须在界面上说出来，否则用户无从判断结果算不算数。
    它长在那枚 chip 上（图标 + 点开的说明），不在结果头上单独占一行：那是关于
    **某一枚 chip** 的事实，而这里正好是能立刻改它的地方。
- **设计系统组件不是 HTML 标签，写之前先 `npx @cloudflare/kumo doc <Component>`。**
  它们常把一件事拆成几份互不重叠的契约，只喂一份照样能渲染出一个"看着完整"的
  元素。`Select` 就是三份：`children` 决定弹层列什么、`items` 决定触发器按 value
  查什么标签、`placeholder` 决定没选中时显示什么——只给 children 会得到一个
  空壳，选中后还会把 `internal`、`12` 这种 URL 原始值直接显示出来。
- **界面回归测试断言的是可见文本，不是属性**（`tests/render.ts` 的 `visibleText`，
  用法见 `tests/rail.test.tsx`）。它先把整个标签连同属性抹掉，只留文本节点。
  这条不是洁癖：上面那个 Select 空壳带着 `aria-label="全部序列"`，按字符串搜 HTML
  会命中，`tsc` / `biome` / `build` 全绿，只有人眼能看出它是空的。凡是"这几个字
  必须让人看见"的地方，就用它钉住。
- **详情栏画哪一份内容由路由决定，不由外壳里的三元表达式决定。** 三种状态各自属于一个路由：
  `s/$turnId/index.tsx` 是没钉住人时的点阵图例，`p.$empId.tsx` 是那个人的时间线，
  工号不在库里是 `p.$empId.tsx` 自己的 `notFoundComponent`。外壳只无条件渲染 `<Outlet />`。
  - **`notFoundComponent` 必须挂在抛 `notFound()` 的那个路由上**，不能挂到 `s/$turnId`。
    挂到外壳那一层会让它进入 notFound 状态，**整个三栏外壳被那一句话替换掉**，
    而它的文案还写着「左边的结果还在」——tsc / biome / build 全绿，只有真访问一个
    不存在的工号才看得出来。要指定由某个父路由接住，用 `notFound({ routeId })`。
- **分层按「谁在用」定，不按「像什么」定**（`-` 前缀被路由生成器忽略，是 TanStack Router 的官方约定）：
  - `src/routes/-components/` 零态与工作台共用的组件；跨路由复用的才进 `src/components/`。
    「跨路由」按今天的使用者算，不按将来可能：证据点阵（`evidence.tsx`）的使用者全在
    这两屏之下，所以它住在 `-components/`；`app-link.tsx` 被 `__root.tsx` 用，才在 `src/components/`。
  - `src/routes/-lib/*.ts` 是页面自己的非组件模块：`view-params.ts`（URL 视图状态
    与到 `SearchFilters` 的翻译）、`filters.ts`（值 → 标签）、`keyboard-flow.ts`、
    `media.ts`（宽窄分界）、`empty-state.ts`（空态引导）、`commit.ts`（提交查询）。
  - `src/components/` 只放 React 组件；纯函数进 `src/lib/`（`format.ts`）。
  - `src/search/` 内部也有一道分界：`result.ts` 是结果形状（页面可以取值），
    `search.ts` 带 `db`（页面只能取类型）。加字段时先问它属于哪一侧。
  - `src/db/index.ts` 只出连接，表定义一律从 `#/db/schema` 取。
  - **页面取服务端的值，只有 `src/server/functions.ts` 一个口子。** 干活的逻辑住在
    服务端专属模块（`search.ts` / `turn.ts` / `llm.ts`），页面一律不 import 它们。
    `createServerFn` 切走的只是 handler 的**函数体**，所以这个文件里服务端模块的值
    只许出现在 `.handler()` 里面。
  - 加筛选维度只改 `-lib/view-params.ts`（类型 + 校验 + `toFilters`）、`-lib/filters.ts`
    与 `search.ts` 的分面；路由的 `loaderDeps` 直接就是整份 search，不必跟着列字段。
- **检索语义有集成测试**（`tests/search.test.ts`）：它在临时 schema 上建一份合成夹具跑真 SQL，
  建表语句由 `schema.ts` 现场推导，不手抄。改 AND 语义、打分公式、路径判定或 `relaxTerm` 之前先看它。
  夹具是编造的人名工号，与人才库无关。
- 提交前：`npm run verify`（= `check` + `typecheck` + `test:etl` + `test` + `build`）。
  `npm test` 需要一个可连的本地 Postgres（读 `.env.local` 的 `DATABASE_URL`）。
