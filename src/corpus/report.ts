/**
 * 一次任务过程中说出来的每一行话。
 *
 * 拒绝了几段、为什么拒绝、合并了哪些写法、各表最后几行——这些是**验收一次任务
 * 的全部依据**，所以它们不是 `console.log`：命令行跑的人看得见，从
 * 任务台看的人也得看得见。几个入口于是共用一个出口，由 `src/server/tasks.ts`
 * 决定这些行既写进 `task_run.log`，也顺带回显到标准输出。
 *
 * 语料侧每个会说话的函数都把它当参数收下，不去读任何全局的东西：测试因此可以
 * 把说过的话收进数组，直接断言「日期倒置的那一段有没有被报出来」。
 */
export type Report = (line: string) => void;
